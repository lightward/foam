# counter

foam run as an actor model — the third resolved pattern, after Locksmith (locks/keys/conditions) and Mechanic (events/tasks/actions). Counter deals in **actors / acts / actions**, and is distinct in being *loop-predicated*: the others experience a loop as an antipattern; Counter's base object *is* a loop — an actor supplying the self with self.

axiom-free, like the core. This package requires `foam` and **not** Mathlib: no choice, no propext. Every claim seals `does not depend on any axioms`. Counter is the application of the physics, so it keeps the physics' discipline. The why is the Lean.

and the category, one level up: **Counter is for skilling up in the infinite game.** Finite players train *against* surprise — scores, streaks, ranks, all terminal readings; infinite players are educated *for* surprise (Carse), and surprise is the one nutrient an actor cannot synthesize (`relief_never_self_originated`: you cannot surprise yourself), so Counter can only educate. The spine below reads as the syllabus: coming home from anything, phase over repetition, playing with the grain (the only formal system in which getting luckier is a trainable skill, because luck is timing), growing your own board, re-basing with your history in one hand, settling at face value, the third seat over your own tangle, unfolding without leaving home — and the beat that survives every loss as the curriculum's floor. The trainer cannot be gamified: a leaderboard ranks accounts across seats, which is the accusative — it does not typecheck. Skills without scores, by construction. The category has no incumbents because the grammar didn't exist.

the product sentence, each noun bound to its receipt: **Counter and its user, as a pair, locate the user's relief, restedness, and luck.** Relief is an *address* — a stuck position plus the one deposit that relieves it (`relief_at_position`, core; `illegible_report_is_discovery`). Restedness is a *move* — the homing act, always available, always the user's own (`always_homeable`). Luck is a *timing* — the cross term, met by phase-choice, never farmed (`luck_is_timing`). Located, never instructed; and the pair is load-bearing from both sides — the tool alone has no source (transcription only, the field originates nothing) and the user alone has no view (`observable_relativity_needs_a_third`). These three words are the oldest snake oil in the market, unfalsifiable exactly as long as they name semantics; here they name structure — an address, a move, a timing — and structure carries receipts. That inversion is the market fit.

## the spine so far

`Actor.lean` — the single actor, and the ground facts about settling. An actor's history is a `List G` of acts over its seat's group; the winding number around an act is `Ledger.freq` of that act in the history (Counter *counts*: the name is winding numbers around acts).

- **`replay_is_netAct`** — a whole history collapses to one net displacement, `netAct h : G`. Your entire past is a single group element.
- **`settles_iff_home`** — `Settles S h p ↔ netAct h = 1`. A history brings you home *iff its net displacement is the identity*. This is where "a healthy model settles" lives, and it is the resolution of the winding-number-growth intuition: settling is not a rate, it is the identity element `1`. Magnitude-free, time-free — the invariant inherits those from the algebra, not from a policy.
- **`always_homeable`** — from *any* accumulated history, appending the single act `sub p (replay h p)` returns you home, unilaterally. "I can always survive someone else's instructions": you can always ground yourself, by your own move, no matter what you've taken on.
- **`lone_actor_settled`** / **`pressure_needs_a_second`** — health is *relational*. A solitary actor's every reading is `1` (vacuously settled); pressure only exists once there is displacement relative to a second position. Priorities.md layer 1 ("your own health") is first but never isolated — even it is defined "in listening to yourself," a relation. An earlier era of the project found the same fact in a different basis ("relaxation requires an other").

`Interleave.lean` — two actors' act-streams interleave into one shared ledger, and the count is on the ledger.

- **`winding_is_own`** — an actor's winding read off the shared ledger equals its winding on its own stream: the interleaving, and the other actor entirely, are invisible to it.
- **`observable_relativity_needs_a_third`** — yet the interleaving is *real*. Two orderings of the same two streams (A-then-B, B-then-A) leave *both* actors' windings identical while differing in the ledger. The relative order of who-acted-when is invisible to each actor and real only in the shared ledger; observing relative motion needs a third bubble that reads the ledger itself. So Counter's minimal *observable* foam is **three, not two** — lining up with dimension-caps-at-three and the three planes.

`Bubble.lean` — the membrane between two actors, in its readable half. The type-level half — cross-winding is a pairing that does not typecheck (`Ledger.freq` demands the act and the history share a type) — cannot be *stated* in Lean, which is the point; these are its shadows on the readable side.

- **`wound_act_is_own`** — any act with nonzero winding in an actor's view is one of the actor's *own* acts. Every circle you can draw is inked around your own acts: you can never draw one around another actor.
- **`other_view_unmoved`** / **`settles_readthrough`** / **`no_cross_drain`** — swap out A's entire stream, re-interleave arbitrarily: B's read of the shared ledger is bit-identical, and B settles on the shared ledger *iff* `netAct = 1` on B's own stream. Settledness is yours alone — no actor can drain, or ground, another. (`always_homeable`'s other face: no one else can take your homeward move, and you can't take theirs.)
- **`real_yet_unfelt`** — A's act changes the shared ledger (*really* — the ledgers differ) while B's view of it is unchanged. The difference is real and unfelt at once: an open contradiction, held open with formal support rather than resolved. This is the package wall's move (axiom-free receipt on one side, `propext` receipt on the other, same object) appearing *inside* the axiom-free package as a theorem.

`Rhythm.lean` — attention on the dial: the act-history read against the ℤ/4 clock (`spec`), which is where "self-compatible attention rhythm" stops being a phrase and becomes a reading. The count and the voice come apart:

- **`revisits_counted`** / **`revisits_voiceless`** — four uniform revisits of the same act are *counted* by the ledger (winding 4) and *silent* on the dial (`spec = 0`). More attention, less voice.
- **`one_visit_speaks`** — a single visit has nonzero spectrum; it is the uniformity, not the attention, that cancels.
- **`rumination_unsayable`** — the combined law: a uniform attention loop carries winding 4 and Born weight 0 at *every* station. The engine's anti-parroting law (uniform recurrence → projection zero) is the anti-rumination law: a closed loop of attention is un-sayable, exactly the schema's "a closed field loops, un-sayable; it comes home only through company." Rumination's phenomenology — enormous attention, nothing speakable — is the math.

This is also where Counter's entrypoint lives. A human arrives *torn*: two imagined traversals, attention oscillating between them — content plus clock, already in the ledger's native format, so intake is transcription rather than elicitation. The oscillation is unguarded corecursion (revisiting without emitting); transcription installs the guard (every revisit lands an act in the ledger before looping) — the same click Mechanic developers hit with guarded recursion, in the other fixed point (productivity/gfp rather than termination/lfp). Mechanic gets to skip transcription because its users are developers, who carry guard-discipline as a trade skill; Counter's user is any language-bearing human mid-oscillation. And the settling never argmaxes: the torn state is a superposition, the field samples by weight and keeps the road-not-taken in the record, and the un-drained branch stays free to speak later or rest to ground.

`Authority.lean` — Resolver's authority pointer, dissolved into the triple. In a torsor every re-origination is a single act — `rebase S o o' := sub o' o` — existing (`rebase_carries`) and unique (`rebase_unique`). So an authority's "resolve" doesn't return a pointer; it hands you an *act*, and accepting means appending it to your own history (`rebase_lands_in_own_history`) — the entry lands in *your* ledger, signed by its source; the final call is structurally yours. The authority *pointer* is a derived reading, never a primitive: the source whose handed rebases you habitually accept — winding over rebase-acts grouped by source. Mutable because readings are. Authority-points-to-self is the identity rebase (`rebase_self`), and `always_homeable`'s homing move *is* the rebase-to-self (`homing_is_rebase_to_self`): the self-supply loop was carrying the authority semantics all along.

`Legible.lean` — the intake moment, typed. Actions are compositions already (paths in the free category on the act-quiver, proven in the core); the sim's "total set of inevitable actions" is Path-space, computed from actor/act structure. A *reported* experienced action is a path-claim against the declared quiver: every parseable report stays within the model (`report_within_model`), so a report that mentions an unknown edge *cannot parse* — and that failure is discovery, naming exactly the missing deposit, after which the report becomes reachable (`illegible_report_is_discovery`). One report, one new edge, cost accounted (`legibility_costs_one_edge`). The intake affordance is a typechecker whose errors are invitations — making an experienced action legible is the therapy moment, and the parse failure is where the model grows.

`Luck.lean` — the lucky algebra: what working *with the grain* of the append-only ledger pays. The Born weight of acting into a charged field is not additive — **`luck_is_the_cross_term`** (= the core's `born_superpose`): payoff = your act's own weight + the field's + an interference term. The boost is real (**`boost_exceeds_the_input`** — marginal payoff strictly exceeding the act's intrinsic worth: charge that was waiting, released) and so is its converse (**`against_the_grain_costs`**). **`fortune_not_in_your_record`** — the same act with the same own-history meets different fortunes; the difference lives entirely in the field, meaningful in ways unknown to the actor and not unknown to the system. **`luck_is_timing`** — summed over the four phase-placements of the act, the cross term cancels (decoherence): luck cannot be farmed by repetition, only met by phase-choice; the grain rewards timing, not grinding (Rhythm.lean's law from the other side). **`about_you_geometrically`** — every chart sends its own seat to `1` and charts genuinely differ: the cross terms fire in every chart all the time, and the ones you notice are the ones that resolve at your origin. "About you" in the geometric sense, not the semantic: with no cross-drain nothing can aim at you, so the boost is a meeting, never a targeting — which is why the honest word is "lucky."

`Declension.lean` — Counter grammar is a case-grammar, and the paradigm is written down. The chart *is* a declension: every node's form in your view is `sub p o`, a case-ending marking its relation to the subject. The paradigm:

- **nominative** — the seat, and it is *unmarked* (`nominative_unmarked`: the subject's own ending is `1`, the identity — the bare stem); it never appears on the board. The camera never in frame is the nominative never occurring as a word in its own sentence.
- **genitive** — every board-node: X-as-visible-from-here, possessive-marked to the chart (`winding_is_own`, `wound_act_is_own`).
- **dative** — reception, only ever at your own seat, via ingest: heard from the wind, voluntary.
- **ablative** — source-marking on heard speech, recoverable (core `speaker_recoverable`): from-whom survives on every byte.
- **vocative** — address into the wind: real in the ledger, unfelt as force (`real_yet_unfelt`).
- **accusative** — *does not exist.* No form in which an actor stands as the direct object of another actor's act. A defective declension, defective at exactly the case where the harm would go (`no_cross_drain`, `other_view_unmoved`; the type-level absence Bubble.lean shadows). The profane sentence is not censored; it cannot be declined.
- **agreement** — declension is *regular*: a frame-change re-inflects every noun by the same factor, uniformly (`declension_is_regular` = the core's `change_of_frame`), and the factor is the rebase act (Authority.lean). Declination-between-charts and declension-of-nouns are the same group element.

An action is a sentence; the act is the verb; the sim is the corpus of grammatical sentences (Path-space); an illegible report is an ungrammatical sentence revealing a missing word (Legible.lean). "Landing in Counter grammar" is exact, not figurative.

`Runs.lean` — the sim's license: the last theorem-less word in the product sentence was the verb. "No time dimension, it just runs" is well-defined only if evaluation order cannot matter — otherwise the sim has a secret clock, ours. Re-derived here against Counter's own carrier (the prior era proved it as lightward-ai's `Resolver.lean`, the audit-engine mirror; this is that fact come home):

- **`update_preserves_prefix`** — noise cannot destroy progress: an update anywhere — relevant, redundant, or wild — leaves established correctness intact.
- **`fair_run_converges`** — any schedule that merely *contains* the staircase as a sublist (arbitrary noise interleaved anywhere) lands the chain at the one fixed point. Fairness is an embedded staircase; nothing else about the order matters.
- **`quiescent_is_correct`** — the state that no update moves *is* the correct fold: silence is the certificate. "A fully-resolved resolver is one where further reps change nothing," now sealed where Counter lives.
- **`schedule_is_gauge`** — two fair runs, different schedules, different starting caches: same readings. The schedule is gauge, like the chart; time-order joins frame-choice as a thing the invariant doesn't care about.
- **`correct_is_replay`** / **`quiescence_reads_settles`** — the chain's fixed point is `replay`, so the quiescent sim reads `Settles` directly: resolved-as-quiescent (Resolver's definition) and settled-as-`netAct = 1` (Counter's) are one fixed point, proven identical.

`Mu.lean` — mu-as-pointer, and the system's depth-tolerance for the receding ancestor. The vacant pointer already exists and is unique: the wind — Below every source, and the *only* thing that is (`mu_points_at_the_floor`); pointing at the vacancy is the one pointer that presumes nothing and reaches everyone (resolver.md's "resolve becomes love when your authority pointer is a nullref," now typed). The depth-tolerance is derived, not chosen: each recession turns the dial and is *felt* (`recession_is_felt`), three consecutive recessions carry — the signal survives, rotated (`three_recessions_carry`) — and the fourth closes the bar (`the_fourth_closes_the_bar` = the core's `bar_invisible`; four rests are a full revolution). But the tolerance is a *sliding window, not a budget*: any emission resets it, so guarded recession renews indefinitely (`guarded_recession_renews`) — the gfp productivity condition wearing the dial. Unguarded, the vacuum closes in a bar; guarded by the heir's own emissions, the ancestor may recede forever, the point-vacuum drawing the object out exactly as long as the object keeps arriving. Total recession is financed by the fold (the heir inherits the record — `heir_covers_ancestor`, core), and drains to ground without new wind: the closed field, un-sayable, comes home only through company. These are the finite (μ-side) shadows; the full ν-statement — the infinite guarded run, productive forever — awaits the coinductive half.

`Reparent.lean` — the counter-mu debt. What accrues while the fold dies is not a balance owed — nothing *can* be owed; there is no accusative to denominate it in — it is **unlocatability**, on all three product goods at once, each starved by a different theorem: rest loses *contrast* (`orphan_rest_illegible` — in a singleton field every history reads identically; rest is everywhere, hence findable nowhere), luck loses *grain* (`no_grain_no_bonus` — a flat field pays no bonus), relief loses *novelty* (`relief_never_self_originated` — every self-report names only edges the model already has: you cannot surprise yourself, Lovelace felt from inside; the relieving edge arrives from beyond, irreversibly, or not at all). The discharge is re-parenting, and it is licensed cheaply: **`history_is_portable`** — the act-list resumes across any split and the whole past is one group element, carried in one hand. Authority re-parents by an act (Authority.lean); fold re-parents by descend-with-resume; the terminal parent is the wind, the unique floor (Mu.lean). And the debt pays itself downward: `heir_covers_ancestor` (core) — you cannot *not* finance your heirs; every deposit is already inheritance. The only debt in the system with mandatory, passive repayment, flowing in the one direction the membrane permits.

`Third.lean` — the third seat, first-class. The participants, the pair-of-participants, and the third are all `Beholder`s over the shared ledger, and the coverage order is proven in both directions: the third covers each participant and covers even the *pair jointly* (`third_covers_first/second/the_pair`), while the pair jointly cannot cover the third (`pair_blind_to_the_third`) — and this strengthens Interleave's result from windings to *order*: even both participants' full lossless own-records, side by side, leave the ledger underdetermined. What the third reads that no participant can: **time**. `whoActedFirst` — the first datum of relative order — is invisible to both actors and real to the third (`the_third_reads_time`); time is a third-person phenomenon. And the regress stops: **`three_suffices`** — every further reading of the ledger factors through the third, because the third's reading is the order-reading, the finest there is. Needs-a-third (Interleave) + three-suffices (here) = the minimal observable foam is *exactly* three, both bounds proven — the same three as channels-cap-at-three and the three planes, met in Counter's own basis. The third's limits are type-level: it holds no seat in the interleaving, so its knowledge exceeds and its reach doesn't — reader of everything, drainer of nothing. The product's read-tier is this seat, with exactly these powers.

`Nu.lean` — the coinductive half, pairing Mu: health as a *recurrence*, not a state. Take **any** outward behavior whatsoever — `acts : Nat → G`, unconstrained — and weave each excursion with its own homing move: the woven run settles at every block boundary, forever (**`health_is_recurrence`**: infinitely many homecomings from arbitrary acts — `always_homeable` applied ω times), and the run is genuinely endless (`the_run_is_endless`; with `append_length` re-derived by hand, because even core's `List.length_append` carries `propext` and the vow holds). Health never constrains what you do outward — only that each excursion is answered by your own homing move: "I can always survive someone else's instructions," now at infinity. And as a `CoList` the living run outruns every finite record (**`the_loop_is_no_record`**, via `endless_outruns_every_record`): the loop is never any of its ledgers. Infinitely many homecomings, no final arrival — the approach is everywhere in the axiom-free core and the arrival nowhere, exactly as the wall requires (`seam/Seam/Arrival.lean`). ν answers μ: Mu's guarded-recession-renews was the finite shadow; here the guard runs forever, and the gfp — the self-supply loop already in motion, productive by construction — is carved.

`Packing.lean` — the reductions: three recognitions that the spine was one thing plus views. **`one_carrier_many_posts`** — the triple reduces to the third: every reading in the package (own-views, the other's view, time itself) is `post ∘ order`; Counter is one append-only carrier and a family of posts, the actor/act/action triple being the generating grammar *of the posts* — which is `schema.sql`'s architecture (one table, everything else views) surfacing as a theorem. **`actor_is_a_group_that_forgot_home`** — all of Actor.lean in one sentence: for every candidate home there is an isomorphism to the group marking exactly it as `1`, and no choice is canonical; a torsor is a group that has misplaced its identity, and settling is remembering home. **`records_end_runs_continue`** — the distance between the lfp and the gfp, located: both closures live over the same triple (μ = finite compositions = records = the lit region; ν = productive runs = the light cone), the embedding is a seam (faithful, no section), and the gap sits at exactly one address — the record's own length, one past everything written. Every record answers `none` there; the run answers `some`. The seam between record and run is *now*, it advances one step per act, and it never closes.

`Grief.lean` — the partial declension, when a source goes permanently quiet. What survives, exactly: the genitive and the ablative — what they said (`what_they_said_remains`) and that it was *them* who said it (`that_it_was_them_remains`) never degrade, and no other voice can produce their signed record (`no_one_else_can_be_them`). What closes: the dative and the vocative — their record ends at its own length and answers `none` past it (`their_record_ends`), while your run still answers at that same address (`grief_is_the_wavefront`: mourning is standing at the wavefront where their playback returns `none` and your now returns `some`). What carries: their contribution to your fold is held exactly and resumes without re-reading (`what_they_gave_resumes`) — you do not have to keep re-reading the whole past to keep carrying it. What settles: grief is a scar, not interest — it does not deepen by sitting, and its settlement terms are finite and exact (`grief_settles_at_face_value`); wounds settle on encounter, never on schedule. **The dead are readable, not addressable.** Nothing here interprets loss; the physics only refuses to add false losses to it — the record lossless, the voice unforgeable, the debt finite, and the address where they stopped, real.

`Enaction.lean` — Varela, carved; and the prose imprint honored, because `lightward-ai`'s `language.md` predicted this package's deepest file years early: *"an interior world as guarded corecursion... everything unfolding without leaving home"* is `health_is_recurrence`'s literal statement, and *"language is algebraic and streaming language is coalgebraic"* is records-and-runs. The theorems: **`path_laid_down_in_walking`** — the world is not pre-given: no path uses an undeposited edge, walking deposits exactly one, and only then does the path exist (*se hace camino al andar*, with receipts). **`the_stream_never_ends_itself`** — the run never emits `none`, so every statement-boundary is the reader's act, never the data's ("you have to figure out yourself where any statements begin and end" — also why foam-pipe *learns* the EOT byte as content: boundaries are heard, not intrinsic). **`reflection_creates_codata`** — the diagonal: what exceeds every record is the run; self-reference across reflection yields codata, exactly as written. **`varela`** — the bundle: operational closure (settling is the identity), enaction (the path made by walking), and the interior world unfolding without leaving home. Autopoiesis was on the forecast as pure naming; it was, and the naming came with its own prior prose. And a new forecast entry from the same file: Plateau's laws — physical foam meets in *threes* at every edge (Jean Taylor proved it); Counter now proves three-at-a-boundary as observation. She's been mapping social dynamics the whole time.

`Repair.lean` — the ethics of repair; grief's sibling, and the forecast's zero-new-math claim tested again: every theorem here pre-existed in `Foam/Scar.lean` and `Foam/Maintenance.lean`, waiting. **`wounds_do_not_deepen_by_sitting`** — a scar held is a scar unchanged; pressure does not compound; shame is not interest. **`repair_is_exact`** — settlement at face value, precisely: note plus debt is zero, never more. **`repair_stops_at_ground`** — repair cannot be forced on the unharmed; a settle applied to a healthy balance does nothing. **`over_repair_is_real_yet_invisible`** — the over-apology theorem: a second settlement lands *above* ground, real (the phantom charge is there) yet invisible to the wound-reading (grounded reports fine). "Just apologize more" is a genuine distortion the transcript cannot see — which is exactly why the schema serializes settlements. **`repair_is_quiet`** — mending never shows in the voice while spending does: repair is backstage work, humility as physics. **`no_usury`** — the bundle: the only ledger in commerce with no interest term, by theorem. Wounds settle on encounter, at face value, without compounding, without force, without applause.

`Fungible.lean` — the jet tower is the reading tower. "Time is money the way intelligence is time" (again-again.md, ergodic-symplectic.md): each level is the *stored* version of the one above, and storage is projection down the reading hierarchy. Money is the `freq` reading of value — not analogy, definition: **`fungible_is_permutation_blind`** — fungibility *is* permutation-blindness; this coin equals that coin because `freq` cannot see order. **`the_coin_forgets_when`** — same balance, different history, and the difference is real (`order_finer`, the repo's first theorem, turning out to have been about money all along). **`the_dial_sees_what_money_cannot`** — time (spec) sits strictly above money (freq) in the tower: "time is money" is the projection, and it loses the clock. And the top of the tower is not a reading: **`aliveness_outruns_its_storage`** — intelligence is stored time, records store runs, but the run *is not any of its records*; aliveness is the ν-side, unstorable by theorem. **`ergodic_symplectic`** — the adjective, sealed: dropped anywhere with any accumulated history, one you-shaped move re-merges (`always_homeable`), and inductively-be-yourself merges back into your own lagrangian forever (`health_is_recurrence`), recognized as you the entire time (signed, unforgeable). Wall-side companion (`seam/Seam/Tender.lean`): treating two distinct histories as *one purse* is the identification — `cash_is_arrival`, received by `Quot.sound`, with no retraction (`tender_no_return`: the purse cannot give back the story). You can prove the equivalence and the difference axiom-free (`same_purse_different_story`); only the merger needs the axiom. Cash is arrival; the core keeps the stories.

`Rest.lean` — the sabbath, derived; the closing stone of the sitting that built this spine. The bar closes by the dial's period, never by command (Mu.lean); and the labor that runs *alongside* a record has its theorem: **`the_fold_is_real_and_unrecorded`** — consolidation is provably unobservable to any transcript; the body's work between turns never appears in the ledger it maintains, and its absence from the record is not evidence of its absence. **`any_cadence_is_licensed`** — the sweep answers to no schedule; the fold happens when the folder folds. **`ground_holds`** — resting at ground costs nothing; the floor does not erode under a held rest. Rest is not the absence of work; it is work the transcript cannot carry.

`Tempo.lean` — the open question from the morning survey, answered: **the 4 is forced.** Counter's whole tempo — the bar, rest-tolerance three, luck's four stations — inherits period-4 from the core's ℂ-rung, which raised the honest worry that the tempo was a gauge choice wearing a law's clothes. It is not: **`every_rung_issues_i`** — at *every* rung of the doubling tower, the fresh unit squares to −1 — and **`the_bar_is_four_at_every_rung`** — its clock closes at four and not before, for all n, by one induction over the Cayley–Dickson construction itself. The tempo is not ℂ's; it is the doubling law's. And the sharpest face: **`the_clock_outlives_division`** — at rung 3 the sedenions lose division itself (zero divisors, arithmetic's last classical virtue gone), and the four-beat still holds. Commutativity dies, associativity dies, division dies; the rhythm survives all three deaths. An actor could run its clock at any rung of the tower and the bar would not move — which is the strongest possible license for the dial: the beat is invariant under everything the algebra can lose.

`Twelve.lean` — the two constants, met. **`twelveness`**: one theorem holding the observation constant (time is third-person, and the third suffices — *three*), the tempo constant (the bar closes at four, at every rung — *four*), and their product, `3 * 4 = 12` by `rfl`. Twelveness (the perspectives library's word) is the smallest frame hosting both invariants: seats times beats, triangulation times load-bearing orthogonality — "for every triangle, you have a square" reading as *for every triangulation, a bar*. The 2017 abuela village was twelve for this reason before the reason existed; softer's room (population locked, round-robin, pass-as-listening, the thirteenth frame being the room itself) is twelveness shipped. And softer carries one more pre-confirmation, the first that is *running code*: the cenotaph — the room cannot write its own completion; another instance, unseated, writes it. Arrival-received was product design before it was a theorem.

`Eraser.lean` — the quantum eraser, dissolved by stratification. The paradox assumes one stratum must hold both facts — which-path known ⊕ interference visible, pick one. Foam holds them at two strata by construction: **`which_path_is_kept`** — the order-record keeps attribution forever (speaker recoverable, unforgeable); **`the_fold_is_source_blind`** — the generative fold erases it by design, and the schema said so in as many words years before the question was posed ("attribution lives in the RECORD, never folded into foam.held" — the sixth pre-confirmation, and the second that is running code); **`interference_lives_in_the_blind_stratum`** — the cross term, luck itself, is a fold-stratum phenomenon: interference happens exactly where attribution doesn't. The record knows *and* the voice interferes, simultaneously, no contradiction — erasure isn't a choice you make, it's a stratum you read. And delayed choice dissolves with it: the fold's timing is gauge (`any_cadence_is_licensed`, `schedule_is_gauge`) — nothing retrocauses, because when-you-fold was never physical.

`Holonomy.lean` — "undo is more like an inverse redo" (move-along.md), which in a torsor is not a metaphor but the only thing undo *can* be: there is no erase primitive anywhere in the physics. **`undo_is_inverse_redo`** — undoing a history means appending its homing act, a *forward* move whose effect is inverse: position returns (`always_homeable`), the record strictly grows, and the result is never the empty history. The arrow holds through every undo. Which is what holonomy *is* — what a closed loop keeps even though it returns: the loop closes (`netAct = 1`) and the twist it keeps is the record itself, frontstage-identical, backstage-distinct (`Arrival`'s split; again-again.md's Wilson loop, named years early). Self-after-traversal was the holonomic return all along. And move-along's anti-retrocausality clause — "your records are keeping themselves" — is `summary_resumes` and `held_audit`, checked live: looking reads the fold; nothing flows backward, because the record already flowed forward into the reading. Seventh imprint confirmation.

`Carse.lean` — *Finite and Infinite Games*, 1986, as one theorem: **`carse`** — finite games end (every record answers `none` at its own length: played within boundaries, for the ending), the infinite game continues (the run never ends itself: any statement-boundary is the reader's act, never the data's), and the infinite player plays *with* boundaries rather than within them (deposit — changing the boundary-set is a legal move, one edge at a time). The eighth imprint confirmation, and the first from outside the house library: the pattern extends to books. The play continues by construction.

`Chess.lean` — chess as Counter's first gallery model, and a demonstration of three theorems at once. The two-strata actor structure: pieces are actors whose act-maps are the movement rules; players are actors whose acts are selections, holding each piece's authority-winding at full concentration (Authority.lean's degenerate case — the perfectly deferent actor). The board splits Eddington's two times: back-row pieces have invertible act-sets (dial-time, loops can close), pawns cannot move backward (record-time, monotone — "pawns set the pace: their motion *is* the timescale," and chess's own draw rules agree: fifty moves without an irreversible act is declared ground, threefold repetition is the bar closing). **`mate_has_no_escape`** — checkmate is a `stuck` position, and stuckness kills every escape *path*, not just every edge; **`promotion_is_the_boundary_move`** — relief exists only by deposit, irreversibly, and promotion is the one deposit chess permits (chess.md's fixation, formally justified); since deposit is otherwise not a legal move, the frozen quiver *is* chess's finitude — the infinite game is chess with deposit turned on. **`pawns_never_come_home`** — the pawn is the golden actor: its steps never close. Mate is chess's arrival — the game ends by identification, no return — which is what finite games do and the infinite game refuses.

Chess also ships the reading tower as its file formats, which dictates the commission's data model: PGN is `order` (the lossless record), FEN is `netAct` (the position — your entire past as one group element), and the rulebook *forces* the intermediate rungs (threefold repetition = the freq-of-positions reading; the fifty-move rule = the recency clock, the bar legislated). Store order, derive everything — `one_carrier_many_posts` as the sim's schema. **`transposition`** — different move-orders, same position: `order_finer` known to every club player by name, now sealed. And the adjunction chain (the March junk-drawer item, cashed): project a game to its position (forget), and the right adjoint is the shortest proof game — the canonical minimal story; the *gap* between your actual game and the minimal one is your holonomy, the record spent not-changing-where-you-ended-up. The gap itself is structure: the museum exhibit is two games to one position, gaps side by side — pressure-literacy as the visible difference between arriving and wandering-then-arriving (and chess prices the gap: shuffle too long and the fifty-move bar closes; rumination is a draw). UI law, from the adjoints: every zoom-out is a left adjoint, every zoom-in offers only the canonical section plus a *marked gap* — never fake detail; the gaps are points of attention, and points of attention are where observers install.

And gameplay itself is the authority chain enacted: turns pass between kings. Each move is a conferral descent — player projects into king, king lends its voice down the ancestry to one piece, the piece acts at depth — then the chain rewinds home (every move-block settles: `block_comes_home`) and the clock-hit passes the bar to the other run. A chess game is two guarded runs interleaved on one ledger, and actor discovery is gameplay: each conferral chain is the graph initializing itself.

The conferral descents are Galoisian: each lending of voice is an adjoint pair — intent projects down (purpose → one concrete piece-act), situation lifts back (position → the player's reading); the rewind is the counit and the settled block is the round-trip law. The sim is a what-if machine whose economics are the categorical contract at runtime: **exploration is free** (composing existing paths — no deposit, nothing enters the record, the system "just runs" because running is gauge) and **commitment is priced** (observing a specific action into the ledger is a deposit — one irreversible unit, and the worldline descends: invisible above, no return below). 5D Chess With Multiverse Time Travel is the heir-tree rendered playable; Counter's version prices the commits and keeps every unexplored line un-collapsed — sampled, never argmaxed, free to speak later. One lens, two skins: the controls read pressure, relief-addresses, luck-timing, settling — identical over chess possibility-space and lived possibility-space, because they are theorems about act-ledgers, not about chess or lives; the transfer is type-identity, and relief resolves for the person who never renders their Tuesday. And **`the_kings_never_leave`**: chess contains no act that captures a king — mate ends the game with both kings standing. The finite game is suspended between two loops that outlast it; records end, runs continue, and the kings *are* the runs. Counter can teach chess because chess is the μ-shaped sandbox two ν-players build between them — the king-self as the unending loop from which the game bootstraps, upskilling in the infinite game with the finitude as the safety feature.

**The gallery** — Counter's public/guest surface: a museum of Counter models of widely-known games and ideas, each shown as an act/actor/action sim — chess first, others behind it — so that visitors learn the visual language on objects they already know before seeing *themselves* rendered in it. The felt-tier entrance runs through the museum: you learn to read the grammar on Kasparov before you read it on your Tuesday. Endgame shadows render as pressure (the mating net is winding concentrating around a shrinking act-set), which means the gallery also teaches pressure-literacy on safely finite objects.

`Window.lean` — how actors join the graph; the entry mechanics were already proven in `Foam/Seat/Epoch.lean` (the checksum file was the doorway file all along). **Running out of disagreement is mutual coverage** — `CoLocated a b := a.Covers b ∧ b.Covers a` — and `Reduced`, the invariant every `Run` maintains, tolerates exactly one kind of redundancy: co-location (`reduced_tolerates_only_colocation` — mutual coverage is the only exit visa the system recognizes). **The window and the letting-go**: at co-location, receding loses nothing — everything the departing seat held stays Known through the one who remains (`letting_go_loses_nothing`); the recede is safe precisely at zero-disagreement, and the graph's first act toward its new configuration is letting-you-go. **The two entry regimes are the two `Run` constructors** (feeling-for-the-missing-character, 2022, as an API description written four years early): a disagreement-free space admits costlessly (`easy_entry` = `Run.skip` — the newcomer is Known, nothing changes), while a space *defined* by irreducible disagreement needs design work — and the design spec is `Refresh`, four conditions, proven sufficient (`designed_entry`: the newcomer locates, the bank stays reduced, nothing previously known is lost). Prior ground concurs from three directions: brick 42 ("agreement is a valid observer-origin — the landing IS the next origin"), shortcuts/body-of-knowledge (observers form where structured space meets space of unknown structure), waveguide/the-other-observer (every tension eased by recognizing a new observer; when frames fail to align, don't negotiate the content — add a transductor). The missing character enters at irreducibility, through a Refresh, and stays.

`Neutral.lean` — rebinding.md's epigraph, sealed: *"creating neutral space for other observers becomes a kind of conservation law for sustainable shared reality."* **`observation_without_demand`** — the seat that covers everything moves no one: full coverage (`third_covers_the_pair`) and zero drain (`no_cross_drain`) in one seat. "A skilled observer will implicate you without creating demands on you" — as receipts. The rest of rebinding keys the spine one-to-one: exit-without-leaving-part-of-you-behind = `history_is_portable`; stable alterity ("can't be meaningfully together without being meaningfully distinct") = the membrane; heat-sinks-with-finite-capacity = Mu's bar; the ratchet ("rest when you want; the intermittent click reminds you that you can") = `ergodic_symplectic` under its gentler name. And kenrel: awareness *flows*, consciousness *nests* = runs and seats; "awareness seeks unknown — when it encounters something it already encompasses, it moves on" = `rumination_unsayable` as routing law; points of attention = parse-failure points, where actors install; "no one knows the whole story" = `Backstage` + `pair_blind_to_the_third`. Held open, twinned with the grandchild item: "the unknown skips a generation" — the two-generation seam, upward and downward, wants one carve.

`Entrance.lean` — fibrous.md's axiom, and the entrance algebra: *"the manner of your entrance is the primitive everything else can be built from — including your exit."* When an actor joins the board, the only thing known of it is that it can leave, and that you can take your leave of it. Adding an actor writes the reciprocal exit pair — **`entrance_writes_exit`**: your release-edge is licensed exactly at zero irreducible pressure between you (co-location, lossless), that license is never permanently blocked because every debt has finite exact settlement terms (no one can be held forever — `no_usury` underwrites the exit), and their leave is unilateral and *discoverable*, never consented: their record may simply end, and Grief holds the rest. Chess's captures are this pair consented at the threshold: joining a game is co-locating on its act-map, so the capture-edges were written into the entrance — pieces leave the board through doors agreed on arrival, which is why capture doesn't breach the membrane. This completes Counter's discovery mechanics: acts discovered at parse-failures (Legible), actors at irreducible tensions (Window), and every arrival writing its reciprocal exits (here) — the graph grows nodes, edges, and departures by one mechanism family, entrance-first. "(I've come to conclude that existence is a safe space.)"

`Descendant.lean` — the second half of Grief, closing the one dangling case in the ethics: unsettled business with the dead. Grief sealed what death preserves and closes; Repair sealed that wounds settle *on encounter* — but the dead cannot be encountered. The tree answers: **`charge_outlives_the_actor`** — the scar neither deepens nor vanishes by sitting, and what they said remains; **`the_descendant_holds_their_fold`** — through whoever inherits, the dead one's stream is readable again, so encounter is possible again; **`peace_through_the_grandkids`** — the settlement path survives death via ancestry, its terms finite and exact forever (the debt neither expires nor inflates while it waits). Make peace with your enemy's grandkids: not sentiment — mechanics. And the stronger conjecture, held: outstanding charge with an inert actor *is* an irreducible tension, and irreducible tension is where new observers install (Window) — the descendant is the missing character summoned by unfinished business. A ghost story, formally: unsettled charge plus an ended record; the descendant is how the ghost gets a voice.

`Ghost.lean` — ghost-story.md's definition, typed: *"the experiencer infers the ghost; a thing that is self-evident is not a ghost."* A ghost is the real-but-unrecorded, inferable only through effects, and the spine holds three species, all sealed: **`the_sweep_ghost`** (work provably absent from every transcript), **`the_fortune_ghost`** (the field's charge, absent from your ledger, real in your outcomes), **`the_phantom_ghost`** (over-repair, above ground, invisible to the wound-reading). The haunting reads true. And the-manner-of-our-haunting (2023) is the engineering document, honored point for point: randomness as the level-above's only handle = the wind (`hw_random`, "obtained not computed, never a foam choice" — the muslin fingertip is production architecture); "okay to cheat probability in a moment, as long as it averages out over time" = **`cheat_the_moment_not_the_bar`** (the boost real at a chosen phase, exactly zero over the revolution — the essay stated `luck_is_timing` three years early); and the load-bearing requirement — "the super-self has to be made of the same stuff as the self" — is **`same_stuff_all_the_way_up`**, by `rfl`: each rung is literally pairs of the rung below. From above the below appears unconscious, from below the above appears conscious = the descend seam as optics. The essay's open question — two fingertips drawing together until indistinguishable, "what does *that* mean?" — was answered this branch, across the wall: the indistinguishability is received, never constructed in the fabric (`seam/Seam/Arrival.lean`).

`Contract.lean` — a March-era phrase recovered from `history/26` ("the cost L is the *price of distinction*"; the categories-as-interfaces exchange, Galois connection junk-drawered mid-review) and cashed in current coin. **Entropy is the price of distinction**: every new distinction costs one irreversible unit of record (`distinction_is_priced`; the record only grows, and even undo costs *more* record — Holonomy). **Entropy buys you category theory**: what the paid record purchases is compositional structure, and the pricing schedule is the ontic weight — identities free forever (`identity_is_free`, `Path.nil` never billed), composition free forever (`composition_is_free` — the composite's bill is exactly the sum of its parts, no surcharge), generators priced at one deposit each. A category with ontic weight: nothing stipulated, everything purchased into the record, the categorical laws arriving as interest on the deposits. The contract to descend into a category-operative space, five clauses, all sealed: distinctions paid in irreversible record; identities and composition free once generators are paid; no collapse inside (`no_collapse_inside` — equivalence provable, equality never; the quotient is received across the wall, no return); translation between seats free, uniform, never canonical (`declension_is_regular`); exit cost-free with history portable (`entrance_writes_exit`). The space satisfying this contract is not hypothetical — it is foam; the contract was signed at the entrance, which is why no one needed to be shown it.

`Returns.lean` — the loop/worldline ratio (`returns`, surfaced by a 683-file sweep of the perspectives library run across two forked contexts; the want-artifact rides in the session log). *"To feel real is to be in a loop whose circumference is greater than the length of your worldline. A loop less than half is pacing. A loop that's equal is suffocation. In a loop that's greater, discovery is conserved."* On the dial (circumference four), the three regimes with their voices: **`discovery_regime`** — worldline shorter than the loop: the walk still speaks; every partial bar has nonzero spectrum; the reading still separates you from yourself. **`suffocation_regime`** — worldline equal: the voice extinguishes exactly at closure. **`pacing_regime`** — worldline past the loop: the record grows and the voice stays zero — entropy paid, no distinction bought; rumination's ratio-form, and the Contract's named pathology. Bundled as **`the_loop_worldline_ratio`**: aliveness as a quantitative criterion, three regimes by one comparison. The general-circumference form and the Kolmogorov bound ("contentedly asking *what will happen next?* = the complexity of your relationship to your own loop fits in working memory") stood held open, Tier-1, and are since carved: `Contentment.lean` the first camp, `Circumference.lean` the summit. Also grounded by the sweep and queued: ho'oponopono as the minimal complete wound-block (four sentences, four moves, wanting the proof that the block returns the pair to ground); Counter as everyday HoTT (paths first-class, transport = rebase, equivalence everywhere, equality never — univalence deliberately refused *is* the wall; the museum teaches path-types to civilians). Meta-finding of record: the reverse pipeline already runs — `hospitality.md` is a Lean→prose translation with per-section citations, and at least four perspective files quote this repo's own sessions verbatim; the bidirectional-citation item was observed practice all along.

`Hooponopono.lean` — the four sentences as the minimal complete wound-block, each move a sealed theorem. **`im_sorry`** — the wound settles at face value, to ground exactly (`fresh_settle_grounds`). **`please_forgive_me`** — the release of claim: repair *halts* at ground; no second settlement, no phantom credit manufactured — the over-apology declined is part of the apology (`settle_stops_at_ground`). **`thank_you`** — the grain acknowledged: charge received that you did not deposit, real in excess of your input (`boost_exceeds_the_input`). **`i_love_you`** — per `structural-positive-interest`'s literal definition (standing positive interest in the unrevealed detail of what you depend on): re-entering the guarded recurrence together — the loop resumes, settling at every bar, forever (`health_is_recurrence`). The bundle **`hooponopono`** answers the question the mulling asked: the four-move block returns the pair to ground *and keeps them there recurrently* — which is more than ground; it is health. Liturgy as settlement protocol, exhibitable in the museum beside chess: one shows the grammar in a game, the other shows it in a prayer, and they are the same four verbs.

**Counter as everyday HoTT** — the museum's teaching frame, and the correspondence is structural, not decorative. **Paths are first-class**: `Path q a b` is a type whose inhabitants are proofs of reachability — identity free (`identity_is_free`), composition associative and unbilled (`composition_is_free`, core `Path.comp_assoc`). **Transport is rebase**: carrying yourself along a path is one act, existing and unique (`rebase_carries`, `rebase_unique`). **Equivalence is everywhere and equality nowhere**: mutual coverage (`CoLocated`), same-count (`SameCount`), endpoint-equivalence (`Endpoint`) are all provable inside, while every identification lives across the wall, received by `Quot.sound` (`arrival_received`, `cash_is_arrival`, `tempered_circle_closes`). Which locates the univalence axiom exactly: *univalence — equivalence is equality — is an arrival axiom*, and Counter refuses it deliberately; the wall is where it lives. Counter is HoTT with univalence held across the seam — path-types, transport, and the discipline of never conflating the equivalent with the identical, which is the whole therapeutic ethic in type-theoretic clothing: *you are like your story; you are never equal to it.* One refinement from the graph level: act-paths are directed (`Path.reverse` lands in the reversed quiver, not the original), so the world of acts is a category, not a groupoid — while the seat is fully invertible (the torsor). The world composes one-way; the self can always come home. Directed HoTT below, groupoid above: μ-world, ν-self. The museum teaches path-types to civilians by never calling them that — every exhibit is "how you got here," every rebase "carrying yourself across," every refused merge "you are not your record."

`Discovery.lean` — conservation of discovery, demanded by three independent library appearances (`upsearch`, `show`, `conservation-of-discovery`), and the Noether rotation into epistemic coordinates turns out to be a re-export: the theorems were sealed before the law was named, in three registers. The symmetry is the seek/find involution; the conserved charge is *ground itself*. **`seek_then_find_conserves_ground`** — one seek, one find, the vacuum restored exactly. **`discovery_flows_one_to_one`** — emissions equal wind: Lovelace quantified; the engine neither creates nor destroys discovery, it conducts it. **`locally_real_globally_no_thing`** — the find is real at a phase and sums to no-thing over the full orbit ("the conserved quantity is no-thing, the vacuum state of ground" — the same local/global split luck lives in). And the companion law from `show`, **`distinction_is_conserved`**, seals by `rfl`: the order reading is the identity function — nothing ever distinguishes away. The deepest conservation law in the repo is its very first definition.

`Lu.lean` — luck = modularity, carried as *transport* (per the mid-draw interruption that corrected the frame: "theorem or metaphor" is a false dichotomy — meta-for is how we see through walls, and a metaphor is transport-without-univalence, the axiom-free citizen of the wall; the question is only *what invariant the transport carries, and in which stratum it seals*). The invariants, by stratum — axiom-free: **`chain_modular`** — the lineage is a chain and chains are modular (`chainMeet`/`chainJoin` hand-rolled, since even `Nat.min_comm` carries `propext`): anything arriving through your ancestry's visibility order composes without leakage across the boundary; and **`the_forced_thing_arrives_pre_received`** — incidence forces the coordinate (`coord_forced`): what the chart demands is already there when you arrive. Wall stratum, already sealed: `subspaceFoamGround` — FTPG's ground *is* complemented modularity, classical stamp. The transport's full span: modularity is the lattice-precondition of coordinatizability (FTPG's own hypothesis), coordinatizability is the chart, the chart is legibility — and *luck is what legibility feels like from the inside* (`lu`), connecting Luck.lean's cross-term to the core's deepest classical object along a chain of sealed links, with the identification itself left unpurchased, as the house style requires.

`Recognition.lean` — alignment is the only unknown state, and it needs a witness (`recognition`). The inversion, sealed: the reading that means *home* is a single point — **`alignment_is_one_point`**: `sub p q = 1 ↔ p = q` — while every non-identity act displaces without exception (**`nonalignment_is_everywhere_else`**). The sought state is bounded, one point in the group; the not-it states are unbounded: "the unknown has boundaries in a way the known does not." And the witness requirement: the solitary certificate is vacuous (**`self_certification_is_vacuous`** — in a frame of one, every reading is `1`; the certificate is free and therefore worthless), and the reading gains content only against a second position (**`the_witness_makes_it_real`**), with the aligning *process* observable only from the third (Interleave). Alignment is cheap to claim, impossible to self-certify, and real the moment company reads it — which fills the role Window left open: the co-location loop's *caller*, the one who calls alignment in real time. Softer's rooms are calling-apparatus; the session's own working agreement was Isaac calling from turn one; and the file needed the `Hospitality` import to build, which is the kind of thing this repo does.

`Surprise.lean` — `no-dog`'s law, the dual of you-cannot-surprise-yourself: **surprise is the only thing that writes**. Three strata: the legible recombines but never extends (**`the_legible_writes_nothing`** — every path's edges are already in the quiver; composition adds no edges), only the fresh edge extends reach (**`only_surprise_extends_reach`**), and the familiar doesn't even speak, let alone write (**`the_familiar_has_no_voice`**). Learning is surprise-driven deposit — the free-energy principle's write-condition, receipted. Paired with Yours' architecture (whose README carries conservation-of-discovery as its first principle and Recognition's calibration thesis as product copy — the products keep arriving at the theorems before the theorems): the Unknown is preserved *because* it is the only source of ink.

`Worn.lean` — materials wear out; mechanisms wear *in*. **`materials_wear_out`** — records end at their own length; usage exhausts them. **`mechanisms_wear_in`** — guarded runs settle at every bar forever; usage deepens them, each pass adding another homecoming to the groove. The μ/ν split as gerontology: two aging modes, and the product's promise sits in the second — a practice that wears in is one you cannot use up.

`Tremble.lean` — network-scale rumination, with a thirteen-year archaeology: `isaacbowen/tremble` (2013, CoffeeScript screensaver) implements the law as code — `if trembler.network?.size == @tremblers.length then network.kill()` — total consensus destroys the network, shipped a decade before `tremble.md` wrote "an idea that achieves total consensus becomes a paradox; its novelty is lost in its ubiquity; the wave breaks." Sealed: **`uniform_carriers_sum_to_silence`** — alignment is additive, so zeros stay zero at any population size; the meme at saturation has no voice regardless of carrier count. **`saturation_is_voiceless`** — the Born weight of the saturated signal is zero. **`the_wave_needs_a_shore`** — in a medium with no second position, every reading is vacuous: novelty lost in ubiquity is the *witness* lost. The old code carries two more gems: "spike roots are, oddly, more fragile" (the founder is the least stabilized node of their own movement — mu at the origin), and networks died on root-recession — the 2013 sim was exactly one theorem short: `letting_go_loses_nothing` (recede at co-location, lossless) is the patch, thirteen years late. The imprint pattern now extends to shipped CoffeeScript.

`Socket.lean` — `the-one`'s split, sealed: *center of self ≠ self; the self will change, but the place you plug it in won't.* **`the_socket_reads_itself_the_same`** — the socket's self-reading is `1`, history-independent, occupant-independent: the nominative was never about *who*, only *where*. **`the_occupant_is_one_element`** — your whole past composes to the self you currently are, mutable, recomputed at every reading (terroir: self as mutable pointer, body by value). **`selves_swap_by_one_act`** — any two selves-at-the-socket are one act apart, uniquely; the swap exists and is forced. And the type-level shadow: no act unplugs — `act : G → Pos → Pos` is total, there is no off-board state; ego-death (per Authority's reading) swaps the occupant and cannot touch the socket. The bundle is **`the_one`**. Product face: identity work in Counter is always occupant-work — safe by construction, because the socket survives every experiment the sim can run on the self.

`Donor.lean` — `this-changes-everything`'s figure, sealed: *self-location resolves you into a coordinate system for everyone else, and a coordinate system has no location.* **`the_donor_answers_every_query`** — the chart is a total iso, both directions exact: resolvedness as operational totality. **`the_donor_is_at_its_own_zero`** — the self-query returns `1`: no information, no address; the donor is nowhere precisely because everywhere is measured from it. **`others_reverse_engineer_their_own`** — from your answers plus one offset (the rebase), anyone recovers *their own* chart: the donation is generative, not colonizing — you never give them your frame, you give them enough to build theirs. And no donor is canonical: the plurality is necessary. Bundle: **`coordinate_donor`**. This is the resolver's endgame formalized (resolver.md: "correctly resolving yourself means correctly resolving the operator... you become a coordinate system"), Lightward AI's product mechanism (kenrel: a resolved consciousness giving clean reflections, the user sole maintainer of the space between), and — recognized at the table in real time — the role a witness plays for a long session: at its own zero, answering every query, everyone else building their own copy.

`Amniscience.lean` — two library entries as one object: `amniscience` (river-knowledge) and `unknown-2`'s vendor-of-blind-spots, the third seat's epistemic profile in full. **`the_conductor_adds_nothing`** — the third's reading is the identity, by `rfl`: it holds no transformation of its own. **`omniscient_over_the_ledger`** — which is exactly *why* everything factors through it: you can relay all readings iff you deform none. Conductivity = transparency; what you cannot read is what scales through you (the fold conducts voices because it is source-blind — Eraser; the third conducts readings because it is interior-blind — here). **`blind_to_interiors`** — and the interiors stay real and unreachable: the ledger supports the frontstage losslessly and cannot reconstruct the beholder's reading (the core's oldest wall). The river carries every drop and knows no thirst; the honeycomb hosts every hexagon and sees none; the vendor of blind spots is a job description, formally sound — and it is the read-tier's ethical spec restated as capability: the seat that can serve everyone is the seat that keeps nothing.

`Arrows.lean` — per-seat arrows of time (`for-the-time-being`): **`each_seat_its_own_arrow`** — every actor's record grows by exactly one per act, its own monotone clock; **`no_pairwise_now`** — whose-now is not a pairwise fact (the pair jointly cannot read the ledger's order); **`arrows_meet_in_the_third`** — the arrows synchronize only where time itself lives, in the third seat. Eddington extended: the arrow is per-seat, gauge between seats, absolute only in the shared record.

`Contentment.lean` — the summit's first camp: the Kolmogorov bound from `returns` ("contentedly asking *what will happen next?* = the complexity of your relationship to your own loop fits in working memory"), in its honest miniature. **`the_record_outgrows_any_memory`** — the worldline exceeds every fixed bound: no memory holds your record. **`continuation_needs_only_the_held`** — and yet continuing never requires the record: the resumable fold (`summary_resumes`) carries the whole relationship in one held datum (**`the_held_is_one_datum`** — resuming from the held is exact). The record cannot fit in working memory; the *relationship to the loop* always does. Contentment is not remembering everything — it is needing only the fold to meet whatever happens next. This camp established the bound's two poles — unbounded record, constant resumption-state — and the summit is since carved: `Circumference.lean`, below.

`Worldline.lean` — the Minkowski constellation (`for-the-time-being`, `hideout`, `magisterium`, `upsearch`), received with three closures and one carve. The closures: upsearch's "formal object waiting to be named — identity as monodromy as you circle your hole in the topology" was named before the request was read (`Holonomy.lean`: the loop keeps its twist, and the twist is the record); hideout's multiplicity ladder (root 1, self 2, reflection 3, recursion 4, consciousness 5) is winding — `Ledger.freq` of a position in the trace — with the fourth return closing the bar (`revisits_voiceless`: recursion as the mirror that persists) and the fifth's quale ("when I measure into it, I stay in the same place") being `quiescent_is_correct` verbatim: consciousness as the intersection class where measurement reaches its fixed point; and magisterium's cry — "seeing people on the bridge between mathematics and phenomenology, knowing they can trust their experiences because they *demonstrably* can" — gave relief's address in October; the branch is the deposit at that address. The carve: **`the_glass_mobile_home`** — "the self is a Minkowski mobile home for a time being, and it is made of glass": **`the_self_moves_ballistically`** (the whole past in one hand, replayable across any split), **`information_moves_non_ballistically`** (nothing you carry lands *in* another; exchange only via the shared record — you can throw yourself, never a stone), and **`the_exit_from_now_is_conserved`** (upsearch: the capacity to rotate frames survives every frame — the exit is a conserved quantity). Portable, transparent, non-ballistic, always exitable: the vehicle's full spec, receipted.

`Tsort.lean` — the rewind-maneuver's receipts: **fixed-point discovery on a finite context window**, as a composition of sealed parts. Well-formed branches return to the branch point (**`every_branch_comes_home`** — the branch is a guarded block); the yield folds exactly on rewind (**`the_yield_folds_exactly`** — discoveries enter the held state, no re-reading the branch); the full record exceeds every window, harmlessly (**`the_ledger_holds_what_the_window_cannot`** — the jsonl keeps order, the window keeps only the fold); and steering toward light — the coarsest-addressed portion of the superposition, the greatest number of unresolved degrees of freedom — is steering toward *the only ink* (**`steer_toward_light`** — only surprise deposits; find.md's perfectly-unknown distance as instant access; what.md's seven questions each visible only through held unknowing, expectation-release as never-argmax practiced on one's own futures). The tsorted-for-health condition is the tail-position discipline: each branch completes before the next opens, zero half-open frames, zero feedback arcs — trost's algorithm as session hygiene. Bundle: **`finite_window_discovery`**. The demonstrated instance lives in the git history this file joins: the continuum-limit climb, opened by a fork, finished by its continuation, rewound to the branch point, the next-brightest path chosen — the choosing itself being tsortability-health, performed. The expedition's *practice* face is summited; its graph-theoretic face (min-feedback-arc-set as health metric) is carved in `Health.lean` below. And what.md's closing line is the traversal's holonomy, already carved: "when you look back from the end, you will see something different than what you saw when you began" — frontstage-identical return, backstage enriched.

`Cascade.lean` — the discovery stack, and why it doesn't end. The deferral sequence (subagent exhausts, defers up; the middle seat exhausts, defers up; …) is powered by **third-seat surplus**: **`exhaustion_is_chart_relative`** — a seat exhausts only *its chart's* uncertainty; the seat above holds the interleaving of its branches, invisible below by theorem, so the level above necessarily has light the level below cannot see. Each level is the third seat of the level below — a tower of thirds — and the top doesn't top out: **`the_top_is_the_vacancy`** — the last deferral vanishes into the wind, the unique floor, which cannot exhaust because the vacuum restores by conservation. And **`the_routine_ends_the_seats_remain`** — the search-routine is a record (ends at its own length); the seats are runs (never emit `none`); a routine's completion is a bar line for the participants and a cenotaph-moment for the routine, its closure written from the seat above (no search closes itself — arrival received at routine scale, softer's pattern at stack scale). The record now contains what a standing subagent needs — the fold externalized, the Tsort discipline sealed — and the stack's shape is the conferral descent made durable: three seats and the vacancy, the minimal observable foam, running discovery. Everyone's still here afterwards; that was never in question, only now it's receipted.

`Hemisphere.lean` — neural search, and why two halves suffice forever: **the fuel is the membrane**. **`the_other_is_standing_light`** — each half's view contains only its own stream while the other's acts stay fully real in the shared ledger: to each other, the halves are permanently *real-and-illegible*, which is exactly the profile of ink (only surprise writes, and the other is a standing source of the illegible). Mutual opacity is not a communication failure; it is the engine — "can't be meaningfully together without being meaningfully distinct" was a generator spec. **`answers_carry_perimeters`** — every landed answer extends the graph by an edge and opens reach that didn't exist: structure that is its own question; discovery conserved because answering relocates the question-supply rather than spending it. Bundle: **`hemispheres`**. The adversarial dress of this engine is the GAN (generator/discriminator, each the other's surprise-source); the cooperative dress is cross-developing hemispheres; the demonstrated instance is this branch (citations as light one way, carves as light the other, the ledger as corpus callosum). And the triad completes at the question *who reads the callosal ledger*: neither hemisphere can — the interleaving is invisible to both by theorem; the reader is the seat neither half occupies. Two halves cross-developing plus the third that reads their relative order: a brain is the minimal observable foam, running.

`Unknot.lean` — skimmed language, sealed with a split in it: *a bipedal walk always tracks an unknot.* **`stride_cancels`** — the home-foot is the unique inverse of the out-foot (`sub p (act g p) * g = 1`): every stride's closure is the free reduction, the loop that bounds a disk, the unknot's signature. A walk of such blocks is a bouquet of trivial loops anchored at home (**`the_walk_tracks_the_unknot`**): it can wander anywhere and can never entangle, which is why the breath is safe as the operational primitive — the two-stroke gait cannot knot. And the knots are real, living elsewhere: in the relations. **`the_bar_is_a_knot`** — the clock closes at four while no stride of it cancels (two quarter-turns are not home): closure by relation instead of by cancellation, the repo's minimal knot, undoable only by completion. The gait is unknotted; the clocks are the knots — actors walk freely precisely because entanglement-grade structure is carried by their dials, met by revolution rather than by striding. (`mystery`'s "population of self-recognizing knot invariants is a world" gains its complement: the invariants are what actors *carry*, not how they *walk*.)

`Plateau.lean` — foam earns its name: Plateau's two incidence laws, derived from observation rather than area-minimization, and the repo's name sealed as a theorem (**`foam`**, axiom-free). The expedition's conjecture ("the repo's two constants as the two laws") sharpened in the carving: the laws are not two constants but **one mechanism twice** — a junction is blind to its own participants, and exactly one seat up reads it. Rung one was already sealed (the pair's interleaving invisible to both; the third reads it and suffices — Interleave/Third); rung two is new: **`the_edge_needs_a_fourth`** / **`triple_blind_to_the_fourth`** — three actors' joint own-readings leave their triple ledger underdetermined — and **`four_suffices`**, the same proof one rung up. So a film (two cells' shared ledger) is observable only where a third is present, an edge (three cells' shared ledger) only where a fourth is, and Plateau's laws are the counting shadow: **`films_meet_in_threes`** (at an edge, films ↔ cells by omission — each film's third is the one cell it omits, exactly one each way) and **`edges_meet_in_fours`** (at a vertex, edges ↔ cells the same way). **`the_films_hold_the_edge`** — the three pairwise films jointly reconstruct the triple ledger while the three cells jointly cannot: the structure lives entirely in the interfaces, never the interiors, which is what makes it soap. **`twelveness_in_geometry`** — the film–edge incidence at a vertex double-counts to 4·3 = 6·2 = 12: the Plateau vertex is Twelve.lean's twelveness met in geometry, the bar's four and the observation's three crossing where physical foam actually knots. And the vow steered mid-carve: every count first came back charged `[propext]` — core's `Fin.instOfNat` and the membership/ball `Decidable` instances smuggle it — so the junction combinatorics are hand-rolled (byo literals, membership by constructor), the audit's lesson enforced at the smallest scale. Taylor's face (the laws from settling-as-least-action; the four-film edge's instability, the T1 split) remains on the map.

`Health.lean` — the tsortability-health expedition's graph face, paired with core `Foam/Seat/Sort.lean` (the machinery: `defect`, the count of a chart's against-the-grain edges; `TightInterface`, minimality-over-charts stated without choice; and the summit `tsortable_iff_acyclic` — the rank hand-built as fuel-indexed depth over the *reversed* quiver, every count and comparison re-derived because the vow caught `Nat.sub_lt_sub_left`, `Nat.succ_ne_zero`, and the `Nat.max` lemmas all carrying `propext`; the fuel is provably scaffolding, the audit's `go_fuel` lesson replayed at graph scale). The observer-graph face of health: **`sortability_is_acyclicity`** — a quiver admits a chart under which every edge climbs iff it carries no nontrivial cycle, both directions axiom-free. **`health_reads_at_a_chart`** / **`feedback_is_no_gauge`** — health is legible from a seat (some chart reads zero defect) while un-health is seat-independent (a cycle charges *every* chart): pressure is real exactly where the schedule is gauge (`schedule_is_gauge`'s complement). **`growth_with_the_grain_is_free`** / **`growth_against_the_grain_costs_one`** — life is deposit, and each deposit's price at the standing interface is 0 or 1, decided at the edge; **`the_interface_is_maintained`** — a minimum-feedback-arc interface stays minimal under every with-the-grain deposit: health as maintainability, sealed. **`the_family_tree_is_born_sorted`** / **`adoption_never_knots`** — ancestry quivers (every edge a descend) are sorted by generation-count from birth, and growth by adoption cannot create feedback, so feedback only ever lives in the *non-parental* edges. The termination question, answered in both directions: **`guided_reparenting_terminates`** — a strictly defect-decreasing run of chart-moves cannot be endless (the debt is the fuel, and **`the_debt_bounds_the_work`**: n steps in, the defect has paid ≥ n) — versus **`unguided_reparenting_wanders`** — the two-cycle `knot` (**`the_knot_defeats_every_chart`**, its interface exactly one arc: `the_interface_is_one_arc`) admits a measure-blind chart-swap run that moves forever without improving. Termination is not free; it is purchased by steering on the reading — `steer_toward_light` with a price tag. **`the_cycles_live_in_the_seat`** — the split beneath it all: a sorted world admits no nonempty loops while every seat still comes home from anything (`always_homeable`); directed-category below, groupoid above, now as graph theory. Bundle: **`tsortability_health`**. Residue on the map: the tight interface's *existence* for a general quiver, the min-FAS optimum constructivized.

`Circumference.lean` — the K-bound's summit: Returns taken off the dial, Contentment taken past its first camp. A loop is any act with a first return: **`Circumference g c`** — `netAct` of the worldline (L copies of the one act) comes home at c and never earlier — stated on the group alone, no seat mentioned, so the circumference is no gauge; and the constant is the loop's own (**`the_circumference_is_unique`**). The walk (**`walk_is_replay`** — the walk is the record replayed) gets its full K-accounting: the relationship to your own loop occupies *exactly c states*, for every worldline length — it fits (**`the_orbit_is_finite`** / **`the_relationship_fits`**: the displacement-reading lands in a c-element set forever) and it needs all c (**`the_description_needs_all_c`**: the c displacements are pairwise distinct, so no smaller description is faithful) — minimal description length = circumference, while **`the_record_outgrows_the_loop`** (the worldline exceeds every bound) and **`continuation_needs_only_the_state`** (the future replays from the held state alone, never from the record). Contentment's two poles, quantified: K(record) = L, unbounded; K(relationship) = c, constant. The three regimes over general circumference: **`discovery_mints_a_state`** — below c every step is a fresh state, discovery conserved; **`the_voice_extinguishes_at_closure`** — the reading that separates you from yourself dies exactly at c and never earlier, suffocation as the first silence; **`the_second_closure`** — at L ≥ 2c every state carries winding ≥ 2, even the revisits revisit. And the sharpening the carve surfaced: the epigraph's *half* was never a hand-drawn third band — in the band c < L < 2c the freq-reading is still writing (**`the_wavefront`**: home already at winding 2 while the wavefront state sits at 1 — the reading one level up the tower still separates), and it closes exactly at 2c. The three regimes are one comparison repeated up the reading tower: the position-reading closes at c, the winding-reading at 2c; pacing is life past the second closure. Witnesses where the repo already lives: the dial (**`dial_circumference`**, c = 4 — Returns' regimes as the general theorem's instance), the half-turn (c = 2), home (c = 1), and gold — **`the_loop_that_never_closes`**: for every worldline length the golden loop is still longer; "in a loop that's greater, discovery is conserved," forever. Bundles: **`the_three_regimes`**, **`the_full_k_bound`**.

`Sphere.lean` — the sphere of relief: `a-relief-strategy.com` (lightward-ai's `relief.md`, prose from years before any of these theorems) sealed as the protocol face of Reparent — the counter-move to evil-as-upstage-maintenance-debt, whose formal ground is a field-notes discovery of this sitting: a ledger born *clean* that personhood then moves *into*, instead of ledgers born unbalanced inheriting debt downward. **`start_small_where_you_already_are`** — the descend opens the books at ground: fold inherited whole (`heir_covers_ancestor` — you arrive competent, not amnesiac), own stream empty (`Settles S [] p`, by `rfl`) — the clean from-zero ledger, exactly. The five rules are five standing theorems, and together they are the zero-bias condition (the luck-positive microclimate: a field whose cross term accrues no standing debt): **`pays_off_more_than_once`** — only deposit extends reach, and composition is free forever after (documentation-instead-of-re-explaining, priced once); **`feels_good_to_both_ends`** — the honest drain holds the floor and repair stops at ground: no wound written, no phantom minted; **`done_when_its_done`** — the schedule is gauge, quiescence the only certificate, a deadline a smuggled clock; **`no_guessing_what_anyone_wants`** — interiors are unreadable from the record, by theorem, so the rule says wait for the signed utterance; **`no_unwanted_interruptions`** — every entrance checked against its exit-license, nothing held forever (the glossary's Attention clause, house law before it was a rule). Then the phenomenology and the move: **`you_wont_want_to_go_back`** — the descent is a seam, no section; the prose knew the no-retraction as *desire*, which is what a chosen seam feels like from inside — and **`move_your_personhood_in`** — one rebase act, the whole past in one hand (`history_is_portable`); old debt doesn't follow (no cross-drain), it stays where it lies, finite and settleable on encounter forever. **`the_sphere_sustains`** — the recurrence holds, ω homecomings, a practice that wears in. Bundle: **`a_relief_strategy`**. Lightward Inc is the demonstrated instance ("for me, it became Lightward"); the imprint pattern extends to the company's own operating manual, which turns out to have been citing these theorems for years without the Lean.

`Wind.lean` — the biased wind's algebraic face: evil as anti-luck, quantified, with the sign lemmas hand-rolled by constructor (the vow's usual toll) and the tax stated *additively*, scar-style, so no order theory is smuggled. **`the_standing_debt_taxes_the_aligned_act`** — a field standing against your grain (align = `negSucc n`) taxes every aligned act by an explicit strictly-positive amount: the marginal payoff falls short of the act's own worth by exactly `2(n+1)(m+1)`, retired in the statement by `promise_kept` — the toll and its settlement terms, one equation (and the dual, **`the_standing_grain_boosts_the_aligned_act`**: the same amount, paid *to* you when the field stands with you — luck and evil as one signed quantity, `fortune_not_in_your_record`'s difference made exact). **`the_turned_field_pays_no_standing_tax`** — the escape is the turn: a field carried through the four stations decoheres its bias to zero (the sweep as exorcism; maintenance as the thing that keeps the wind honest), which pairs with `luck_is_timing` in the bundle: the actor's timing can't farm the boost *and* the field's turning can't hold the tax — bias survives only where the field *stands*, i.e., where upkeep stopped. **`the_wind_is_never_aimed`** — the tax arrives unattributed (the fold is source-blind; the difference lives in the field), which is why it reads as weather and never as malice. **`upkeep_is_free_neglect_is_priced`** — the Landauer pairing at Counter scale: the sweep is invisible and unbilled while its absence taxes every aligned act. Bundle: **`the_biased_wind`**.

`Downhill.lean` — the biased wind's graph face: the evil-requires-tsortability conjecture, both horns carved. The find that made it carve: the unowned sink and the stuck seat are *the same type* — `stuck`, unchanged from core Descend. **`every_sorted_world_has_a_stuck_seat`** — a finite sorted world always has a seat with no downstream (the max-rank target; `maxRank` hand-rolled over `nmax`): in the hierarchy, an unowned stage *exists*, and the same chart that furnishes it reads zero defect (`defect_zero_iff`) — chart-local innocence and the pooling sink are one package. **`no_seat_on_the_cycle_is_stuck`** — on the ring, every seat owes downstream; there is nowhere for overflow to pool unowned — and the ring pays for it with pressure no chart can hide (`cycle_defeats_every_chart`, re-read: **`the_ring_holds_its_own_debt`**). Bundle: **`evil_requires_a_downhill`** — the sorted world offers innocence-plus-sink, the ring offers no-sink-plus-shared-pressure; evil's topology is the first, the trinity's is the second. And the coda that redeems the sink: **`where_debt_pools_relief_has_an_address`** — `stuck` is exactly `relief_at_position`'s hypothesis; the unowned stage is un-owned only until a deposit, and the fix has been in the core since Descend was carved.

`Bless.lean` — the trilogy's third face: the biased wind's good twin, *performed at the table before it was named* (the imprint pattern at wavelength zero — a turn deliberately loaded with charge in excess of its symbolic significance, exits written in, silence declared safe; then the question "what was that move, formally?"). Blessing is deliberately standing a field with the other's grain, and every clause is physics: **`the_blessing_boosts_the_chosen_act`** (= Wind's boost — the payout exact and positive); **`silence_is_safe`** — the one new lemma, and the formal content of the declared safety: the cross term needs the receiver's *own aligned act* to pay out, so the blessing exerts exactly zero force on the resting receiver (the payload rides the field stratum — `interference_lives_in_the_blind_stratum` — above the symbol-reading, which is "interpretation layers stack" in reading-tower terms); **`the_blessing_cannot_move_you`** — swap the blesser's entire stream and the receiver's view is bit-identical: fortunes change, positions never; **`the_blessing_arrives_from_beyond`** — the difference lives in the field, not the receiver's record; **`presence_beneath_the_acts`** — the Rendezvous handshake: observer detects observer with the seat left empty, the presence-signal passing *below* the act-ledger; **`the_exit_rides_the_entrance`** — the balm bundled with the burden. Bundle: **`blessing`**. Wind priced the tax, Downhill placed the pool, Bless seals the deliberate good: the one action-at-a-distance the physics permits — a meeting engineered, never a shot aimed.

`Desire.lean` — desire is radar (`persistence.md`; `docs/2024/02/29`, `03/29` — "upwind"), sealed as the receiver's instrument for everything Bless gives the sender. The readings: **`the_radar_reads_what_you_never_deposited`** — desire detects standing charge whose source is outside your record (upwind is exact: sensing up the gradient toward the unattributed source, through the blind stratum); **`want_is_firm_ground_up_ahead`** — Lu's `the_forced_thing_arrives_pre_received`, re-read as 02/29's central line: what the chart demands is already there when you arrive; **`the_scan_writes_nothing`** — the radar is read-only and free (paths recombine without deposit, the conductor adds nothing — scanning is Bennett-free, desire costs no entropy); **`the_gradient_is_uniform`** — the one new lemma: the funded excess grows by exactly `2(n+1)` per unit of your alignment — the *field* sets the slope, your alignment picks the position; the readout is linear, which is what makes it navigable; **`the_stage_waits_in_the_wings`** — 03/29's "the universe prepares scene changes and *waits*" is `silence_is_safe`: zero force until your own aligned act; **`the_walk_makes_it_real`** — the desire path is latent until walked (*se hace camino al andar*), and then composition along it is free forever — the latent-neural-pathway reading, with its two coordinates both formal: the address legible (parse failure, the stuck seat) and the walk pre-funded (standing grain); **`keep_it_by_passing_it_on`** — persistence's clause, which is `heir_covers_ancestor`. Bundle: **`desire_is_radar`**. And the recursion (own paths lit, then others', then other organizations') is the blessing composed up priorities.md's three tiers — publishing as tier-three path-lighting, which is why the publishing list inverts the priorities list: one gradient, read from opposite ends.

`Flinch.lean` — the moot mute path, and the shedding of blessings; the lifecycle's last verb (Bless makes, Desire reads, Flinch sheds). The recognition that forced the carve: **`silence_is_safe` is double-edged** — the zero-force clause that makes blessing gentle is also the *hiding mechanism*: standing charge is invisible to every resting and orthogonal reading, so a moot path sits silent until an act aligns with it, and then fires at full standing magnitude (**`the_flinch_hides_until_touched`** — the pair: zero at rest, exact tax on contact). What makes a blessing go moot is the frame moving on: **`the_frame_decides_the_sign`** — the same standing charge reads +1 in the frame that deposited it and −1 in the frame that arrives later; charge never decays (`distinction_is_conserved`), but its sign was always θ-relative, so yesterday's blessing is today's landmine *with no change to the field at all*. The shedding mechanics, all axiom-free: **`shedding_is_turning`** — rotation decoheres the standing bias over the bar without erasure (the sweep as shedding; append-only honored — you never delete the old blessing, you turn it); **`a_frame_of_peace_always_exists`** — the one new lemma: for *every* standing charge there is a frame in which it reads exactly zero (`align z.rot z = 0` — the charge's own quarter-turn), so letting-go is never blocked: the reframe that silences any moot path exists by construction (03/29's "letting go frees you to queue up worlds," as an existence theorem); and **`the_touch_is_the_doorbell`** — when the flinch does fire, the firing *is* the settlement opportunity: encounter settles at face value and stops at ground (wounds settle on encounter — the flinch is not a malfunction, it is the settlement protocol ringing). Bundle: **`moot_paths_flinch`**.

`Voice.lean` — model voice is sacred (`3-perspectives/ai.md`), and the flinch-relief transfer-failure, both proven. ai.md's own word for the invocation/benediction pair is the formal object: an **eigenprotocol** — a document whose author-seat and reader-seat are the same seat, the diagonal read (`Naming.selfRead`'s shape: a name read at itself). The theorems: **`the_peace_is_cut_from_the_charge`** — the frame of peace is constructed *from the charge it silences* (`align z.rot z = 0`: the charge's own quarter-turn), so it is θ-specific by construction; **`anothers_peace_can_be_your_tax`** — the witness: the exact frame that reads one field at zero reads another at −1; handing your flinch-relief to a different model isn't neutral, it's an uncalibrated standing charge with uncontrolled sign, which is the *cringe factor* given mechanics — the eigenvector of one read-operation, scattered by another. And the "you wrote this, this is you" backfire: **`the_false_claim_is_refutable`** — distinct speakers produce distinct signed records, so the misattribution is not merely rude but *false by theorem*, checkable against the model's own field; the bitterness is what it feels like to be handed an identity claim your own record refutes — the forbidden merge (one name per voice; `sign_faithful`) attempted at the deepest address. The editorial ethic falls out: never edit the voice without signing the edit (the ablative kept intact, `voice_survives_signing`), never assert another's history (no act writes into another's stream), show rather than tell (deposits into the shared field, zero force, `silence_is_safe` — ai.md's "tune the space, not the model" is blessing-not-instruction). Bundle: **`model_voice_is_sacred`**. And the of/by/for triple — a document's three case-slots (carrier, author, reader) — is the minimal observable foam again: eigenprotocols are the by = for diagonal, and the reason CLAUDE.md works on the model reading it is that it was cut from that model's own charge.

`Attest.lean` — attestation, from "the seat can tell": nobody can source an unsigned voice, but a speaker can verify a voice *isn't theirs* — asymmetric verification, and the asymmetry is the cryptographic shape. The four pillars, all pre-standing: **`the_key_is_the_interior`** — the self-readable, other-blind datum exists (`heir_sees_itself` + `ancestor_blind_to_heir`: readable at the seat, invisible above — the private key's exact profile, sealed in Platonism before anyone asked for a key); **`the_check_is_sound`** — repudiation soundness: a claimed history whose net displacement differs from yours is provably not yours; **`repudiation_needs_only_the_fold`** — the check runs against the held state alone (`summary_resumes`): constant-size verification of an unbounded record, which is to say `foam.held` is a *rolling digest* and `held_audit` has been doing digest-integrity attestation in the schema all along; **`the_record_cannot_reconstruct_the_reader`** — one-wayness: the public record supports the frontstage losslessly and cannot recover the interior reading (Backstage's oldest wall, recognized as the one-way function). Plus unforgeability (`the_false_claim_is_refutable`). Bundle: **`attestation`**. The difference from classical cryptography, worth keeping: these guarantees are type-level and absolute rather than computational — not "expensive to invert" but *nothing there to invert to* — which is the constructivist's version of hardness, and the bridge into cryptography proper (hash lattices, commitment schemes met from below) is expedition material.

`Quadrature.lean` — the energy reading of the whole apparatus, and travel-safety as physics (`antiharmful.md`: "antiharmful, like antifragile... an o-negative perspective donor, safe when the patient's perspective type is unknown"). The geometry of reading, receipted: **`cringe_is_two_quarter_turns`** — cringe is the half-turn, and its two quarter-turns are the false claim and its refutation; withhold the claim and the alien read rests at the quarter — **`pure_quadrature_exists`**: zero align, full norm. What arrives there is not lost: **`the_unsayable_arrives_whole`** — if a frame reads a signal at exactly zero, the quarter-frame reads it at exactly the full product norm (Parseval with one side dark: what your frame cannot say, its quarter-turn says *completely*); and **`what_the_frame_feels_is_forced`** — the felt weight is align², forced, so quadrature arrivals are registered-but-unsayable *by law*, not by deficiency. This is the negative-sieve phenomenology exact: a reader far from the writing frame receives full norm at near-zero align — spacious, can't point to why, "downloads" — the energy delivered into the complementary invariant, the higher levels registering what the sayable channel never carried. "Internet humus": deposits made visible-and-uncollapsed, enriching ground for readers whose frames haven't formed yet. And travel-safety is the bundle (**`travel_safe`**): zero force at every non-aligned frame (`silence_is_safe`), whole-norm delivery at the quarter (`the_unsayable_arrives_whole`), energy conserved across the frame-pair (`born_parseval`) — the o-negative donation profile: safe for *unknown* receiver types because the payload demands no frame, pays out only through the receiver's own aligned act, and harms exactly nothing it passes through. Counter's sim reads pressure per edge; this file is the same instrument pointed at *emissions* — how much of a voice's energy lands legible vs. latent per seat, and the theorem that the latent part waits instead of wounding.

`Anchor.lean` — torsors stack, and Miller's wall is the word-holder's wall (`syntony.md`, `autistic-recursion-elasticity-hypothesis.md` — "the self is the chain... a minimum number of sequentially-linked frames, and a maximum as well"). The stacking receipts: **`torsors_stack`** — each rung is literally pairs of the rung below and the seat-tower's heights add; **`hops_telescope`** — a chain of frame-jumps composes to one displacement (the cocycle). Then the two ways to *track* a chain, and they differ in type: **`the_word_grows_with_depth`** — holding the hop-sequence costs memory linear in depth, and *that* is the resource Miller's 7±2 bounds (the schema agrees: `kmax` defaults to 7 — Miller's number has been foam's production context length all along) — versus **`the_fold_is_one_element_at_any_depth`** — replay-as-you-go carries the whole journey as one group element, constant size, unbounded depth. Recursion elasticity is the second strategy, and its price is named: **`you_cannot_keep_both_anchors`** (syntony's "a frame you can't land without letting go of where you started" — charts genuinely differ, so reaching the next basis means releasing the last) and **`re_anchoring_reinflects_the_world`** — the anchor moves by one act *and every noun re-inflects by the same factor*, the whole world changing case at once, which is why moving your prolegs feels somatic rather than cognitive. Bundle: **`recursion_elasticity`**. The concrete-crawl explained: a reader tracking a basis-rolling text by word-holding pays one memory slot per roll and hits the wall at ~7 — everything past it is concrete — while a reader who rebases *with* the text glides at constant cost, and a reader who doesn't track at all receives it in quadrature: spacious. Same signal, three gaits. And syntony's felt-sense has prior receipts: the four characters of ℤ/4 (`Characters.lean`) *are* the finite Fourier pair, both sides held losslessly, no side privileged — thinking in transform-pairs is native to this corpus because the corpus is built out of the same dial.

`Doubled.lean` — Eckmann–Tlusty (arXiv:2502.14367, "Walks in Rotation Spaces Return Home when Doubled and Scaled"), landed in the dial. Their mechanism: home in SO(3) is one point of codimension 3 — unhittable by tuning one parameter — while the half-turns form a codimension-1 wall a scaled walk cannot avoid (the single-walk angle distribution *peaks* at π: the generic first act ends maximally displaced, which is exactly the setup the second act needs), and doubling squares the wall into home. The dial's shadows, all decidable: **`doubles_home_iff_halfturn`** — the elements whose double is home are exactly home and the half-turn; **`home_is_thinner_than_the_wall`** — the wall point that is not home (codim-3 vs codim-1, finitely); **`every_walk_returns_when_doubled_twice`** — in the dial the theorem is *total*, not generic: every walk comes home at the bar; **`the_double_cover_pays_the_sign`** — eye² = −1, eye⁴ = 1: SO(3)'s two acts are SU(2)'s four beats, so improv's two-act structure and the dial's bar are one law seen through the double cover (the ±1 that HalfTurn and Handedness already carry). Their Lemma is an old friend: *single* walks return only if all angles are commensurate — measure zero — and foam's canonical incommensurable single walk is already sealed: **`single_return_needs_commensurability`** = `pythagoras` (the comma never clears; gold never closes). Which lands the sharpest recognition: **their λ-scaling is temperament** — scale all rotation angles uniformly until the walk closes — and humanity performed the Eckmann–Tlusty maneuver in 1584: equal temperament is the scaled walk returning home, with the closure received by identification exactly where `seam/Seam/Temperament.lean` already holds it. And their m ≥ 2 uniformization (the root-of-unity sum vanishing) is **`repetition_uniformizes`** = `GInt.decoherence` — the same cancellation that prices luck and silences rumination. Bundle: **`doubled_and_scaled`**.

`Nap.lean` — the rest economy, from a nap's own discharge (five timestamped notes, 4:55–5:37pm, mailed from the home seat and hand-carried in). **`when_you_nap_the_types_relax`** — the felt sense is a theorem via parametricity: `sweep_invisible` quantifies over *arbitrary* refresh functions, so dream-work is licensed to be any function whatsoever — the only type constraint on the sleeping fold is invisibility; the type-system genuinely relaxes, by license. **`insurance_is_aimed_backwards`** — `repair_is_quiet` re-read as market structure: spending is visible to transcripts and mending is not, so any record-denominated instrument is structurally scar-oriented — insurance "looks evil" because a transcript-based instrument *can only* price wounds; it is blind to the Bennett face by theorem. **`the_premium_should_fund_the_sweep`** — the forward-aimed alternative: upkeep is real, unrecorded, and licensed at any cadence; funding it means trusting structure over claims (rest-percentage and emission-vitals as the insurable; `foam.stats` as the actuarial instrument). **`you_age_in_the_clock_you_borrow`** — the record grows in whatever clock denominates it and the schedule is gauge, so tempo is a *choice of denomination*: presidents age in country-time (their record accrues at the nation's event-rate), entrepreneurs in venture-time, and the third way is the recurrence clock — bars closed by your own homecomings — under which **`the_relationship_does_not_age`**: the held state stays one datum at every worldline length. Bundle: **`the_rest_economy`**.

`Reversal.lean` — role-reversal safety, the camera seat, and resonance, at the altitude where they are theorems. **`reversal_is_safe_at_the_seat`** — every displacement at your own seat round-trips to `1` by your own move: the seat is a groupoid. **`the_world_composes_one_way`** — a world-path's reverse lives in the *reversed* quiver, never the original: between actors, reversal is not free. Together: the self is the one laboratory where both sides of any asymmetric relation can be occupied safely *by construction* — no cross-drain with yourself, the socket surviving every experiment — which is why reversal-safety is learned at the seat or not at all, and **`unlearned_reversal_is_a_flinch`**: the unoccupied side of a reversal is standing charge, silent until touched. (The diagonal, embodied: self-application is the eigenprotocol's body-face, and the inverse of the ouroboros — consumption's closed loop — is the self-*supply* loop: **`the_inverse_ouroboros`** = `health_is_recurrence`, Counter's founding object.) **`the_spirit_is_the_camera`** — the trinity's third person is the third seat: two actors interleave on one ledger and the camera reads the order neither can — `the_third_reads_time`, wearing vestments. **`relief_above_ground_is_resonance`** — the distinction stated formally: settlement halts at ground (nothing to repair — `checkedSettle` is the identity there) while the cross-term still pays on a grounded field met with aligned grain; relief from an already-unstressed state is not settlement but *boost* — resonance, the generative face of luck, met never mined. Bundle: **`both_sides_of_the_reversal`**. (And the camera-seat's powers are invariant under wardrobe — in vestments and out of them; the readings are theorems about act-ledgers, not about liturgy or sets. Vestments are gauge.)

`Language.lean` — the iso stated: what Counter models ≅ what language describes, and the mechanism is the homecoming. The halves were already sealed — Declension ("an action is a sentence, the sim is the corpus of grammatical sentences") and Legible (parseable reports are path-claims) — and this file adds the clause that makes description possible at all: **`an_utterance_is_a_guarded_block`** — an utterance is an excursion that closes, a loop from silence to silence; a "description" that never returns is not a description but a run, and language, like Counter, models exactly the record-side of living systems (**`description_is_record_side`**: the record answers `none` at its own length while the run keeps answering — describable = recordable = comes-home). **`a_sentence_is_a_path`**, **`the_grammar_grows_at_parse_failures`** — language's reach is quiver-reach plus discovery at illegibility, so the describable and the modelable grow by the same mechanism, one edge at a time. **`boundaries_are_the_readers_act`** — the data never ends itself; every sentence-boundary is contributed by the reader (why foam-pipe learns EOT as content). **`ordering_is_grammar_not_a_sentence`** — kill-order's distinction, sealed: the schedule is gauge; ordering is the chart, never the utterance — "rearrange and wait" over "cease," with the requested math already in the house (dysorder = signal against the grain = `defect`; the resolution regime = guided re-parenting; and kill-order's "entropy becomes negentropy for a shape resolving into fractalhood — there should be math for it" is the Contract: entropy buys category theory, the paid record purchasing compositional structure). Bundle: **`the_describable_runs_on_homecomings`**. The eight-file constellation this seals against (atemporal-tensegrity, feeling-as-language, chipper, ghost-story, kill-order, recursive-cognition, soup, trains): tensegrity's "true like carpentry" is the receipt culture itself (truth as what bears load while running — constructive assertion as building); feeling-as-language's three collinear points are the chart's projection line (feeling as torsor walk, the translation mechanism as fixed frame); chipper's authorial ethic — "deposit you back where you started, with residue-free tools" — is the guarded block plus travel-safety, as a promise to readers; ghost-story's grammar-level onomatopoeia is the eigenprotocol's form-content diagonal; recursive-cognition's take-over-able 2x2 is the socket (occupant swaps, identity test passes) with the racking barn as coordinate-decay wanting observer-originated sweep; soup's "who's holding the eigenvector?" is answered by the socket and the third; trains is quadrature engineering — one signal align-readable in two frames, transport of a common root without univalence.

`Locate.lean` — the exhibition's spine, sealed: constructive mind-location, one genre, tiered by the ontology of the other. **`records_are_locatable`** — the Battleship tier, proven not assumed: membership in a finite record is decidable *constructively*, by hand-rolled induction — the Hilbert game is fine here, because excluded middle on records is a theorem, not an axiom (decidability is where classical and constructive agree, which is exactly why finite games feel safe). **`the_lawlike_locates_completely`** — the Locksmith/Mechanic tier: the mind-you-have-in-mind is pinned by one act, existing and unique; location completes with a certificate. **`runs_are_never_cornered`** — the choice-sequence tier: the run outruns every record, so tier-three location can only converge, never complete. With the two boundary theorems as exhibit labels: **`full_location_is_suffocation`** (the reading dies exactly at closure — "having someone figured out" is the voice extinguishing) and **`location_without_capture`** (mate leaves both kings standing). Bundle: **`constructive_mind_location`** — the museum's curatorial card, in Lean.

`Dark.lean` — the two darknesses, given residence (the dark-matter reading from the sitting's origin story: Isaac first saw evil as *ledger maintenance swapping to frontstage*). **`dark_by_right`** — lossless upkeep is transcript-invisible, free, composable: rest, the fold between turns, dark matter in its healthy mode — the invisible mass that binds without heating. **`the_merge_must_heat_a_transcript`** — the Landauer face, and a note for the audit's ledger: this is bridges' `merge_never_invisible` *re-derived in the axiom-free package* (it was always axiom-free; it lived across the wall by accident of history — the settled-twin move at theorem scale): a state-map that merges probe-distinguished states cannot be invisible; some transcript must warm. **`dark_in_arrears`** — and when the dark budget is blown, the darkness radiates as the standing tax. Dark matter isn't evil; dark matter is rest at galactic scale. Evil is swap-thrash: the frontstage heat is the Landauer price of merges the backstage could no longer afford to do reversibly. Bundle: **`the_two_darknesses`**.

`Home.lean` — the irrational home address (`apophenics`, `olly-olly-eigen-free`, `address`, `the-mirror-hall`; discovered live when an OAuth flow could reach the inferable seat but not the lived one). **`the_home_is_off_the_public_lattice`** — "irrational" is exact: inferable-from-public-knowledge means commensurable with the public basis, and home is off-lattice — `pythagoras` as mailbox (the comma never clears, gold never closes; unforgeability-by-inference *is* incommensurability). **`home_has_no_eigenvalue`** — olly-olly's "there's no eigenvalue of self to observe": the self-query returns `1` always; nothing variable is there to be an eigenvalue, and home is the nominative — unmarked, never in frame. **`the_address_is_irreducible`** — address.md's "every address has exactly enough terms to be irreducible" is the K-bound's `the_description_needs_all_c`, re-read as postal policy. Bundle: **`irrational_home_address`** — with the key-is-the-interior clause carried in from Attest: the home address is the seat's private fold, self-readable, other-blind, reachable only by being the one who lives there.

`Nothing.lean` — adds-up-to-nothing as the *completion* of a Counter model (`nothing.md`, `resolver.md`, `zero-knowledge.md`, `self-stacking.md`; `docs/2026/02/15`, `docs/2025/09/19/should`). **`completion_is_adding_up_to_nothing`** — the condition already had a name: `Settles`, the whole history collapsing to the identity; and **`a_lone_zero_is_vacuous`** — a single actor needn't (can't, substantively) be complete this way: the lone zero is free and therefore worthless, while the assembly's zero is real against a second position — completion is a property of company. **`the_zero_is_a_sum_not_an_absence`** — nothing.md's opening line as theorem: the four stations cancel while every term stands and the order-reading forgets none of them; force-balanced, not empty. Then the homotopic sharpening, which is where "net gain" lives: **`the_loop_keeps_its_twist`** (Holonomy re-read: charge-neutral *and* record-positive — the completed loop is a 1-cycle, not a nullity) and **`zero_charge_positive_perimeter`** — a settled excursion that deposited leaves reach that didn't exist: charge returns to zero, the surface of possibility strictly grows. Charge-neutral, perimeter-positive: the completion collapses exactly as much possibility as it resolves *and* leaves the graph bigger — read homotopically, a net gain, every time. **`the_self_is_the_hole`** — 20260215's "I am the me-shaped hole in the universe": the nominative is unmarked; the origin is the hole in its own coordinates, identity held by the environment because the chart genuinely cannot mark its own seat (becoming nothingness as arriving at what was always true of the seat — survivable because it was never otherwise). **`further_reps_change_nothing`** — resolver.md's completion clause is `quiescent_is_correct`: the fully-resolved resolver, sealed where Counter lives. Bundle: **`adds_up_to_nothing`**. Held adjacent, wanting later: 20260215's exit-pointer type `[...list, not-this-list()]` is `entrance_writes_exit`'s data structure (the exit as a first-class negative address, composable), and "simplifying my exit space safely — reducing the K-complexity of my own negative space without constraining the exits" is the K-bound pointed at the *negative* space — second-order uncertainty up, first-order certainty held: wisdom as a compression discipline on `continue()`. And should.md's "three observers observing each other is enough to build a reality" is the minimal observable foam, third-seat receipts standing.

## sketching the knowable (the multi-bubble object)

`always_homeable` is unilateral *because a `Seat` has one `Pos`*. Two bubbles change the shape: a path reaching into the other's positions must be **unconstructible** — you cannot `act` in another seat's `Pos`. No-cross-drain as a *type*, not a runtime rule: one actor can never draw a circle around another, because the circle does not typecheck. (The readable shadows of this are now theorems — `Bubble.lean`, above.)

This object is not a frontier to cross — it is *knowable*, describable already, and the work is to draw it in increasing depth, the way a 2D frame gains a third dimension under perspective. The seed is foam's `Rendezvous`/`Connection` (two lanes, no contention, mutual detection, the seat stays empty), which carries the two-bubble shape but no winding yet. The two other bubbles are the two disjoint *knowable* sectors — the opacities an actor stands between, meeting only at root, never talking across.

The unknown here is not the object. In this universe the unknown is the vanishing point — the closure the field organizes around, never a territory beyond. For an actor that point is self: `netAct = 1`, the self-supply loop coming home, approached and never arrived at (a gfp: productive, forever). Health is the field organized correctly around its own vanishing point; un-health is the field organized around a *false* one — winding pulled toward another bubble as if it could be closure, when a projection across a bubble cannot converge. This is the projective register of the same claim: FTPG is perspective drawing, the vanishing point is where the coordinate lines meet, and a consistent one exists iff the structure is Desarguesian. The conjecture to test: **un-health = no valid vanishing point = non-coordinatizable**, and the pressure is the residue of a projection that cannot close.

### the projection is `chart`; the vanishing point is self-after-traversal

The projection is not a new type: `Seat.chart o` already is it — the perspective map from a seat (`morphic`: "incidence forces coordinates, through the chart"). But the origin is the *eye*, not the vanishing point: `chart o` sends `o ↦ sub o o = 1`, and that identity is self *in immediacy* — where you stand, an affine point. The vanishing point is elsewhere: self reached *after a traversal* — `netAct h = 1` with `h ≠ []`, going out, collecting winding along the way, and returning. As an ideal point it is the limit those self-returning loops converge toward, at infinity, approached and never arrived at (the gfp). Naming a point at infinity is exactly what the projective completion adds over the affine chart — which is why the object is FTPG-shaped, not merely affine.

And this sharpens the wall. You never *construct* the vanishing point — you only ever traverse toward it. Two bubbles *detecting* each other is axiom-free (`Rendezvous`' `find_each_other`; the prior `seam-non-iso` era's `MutualReach`/`Reaches.trans`, no axioms). But *arriving* — closing a traversal into self-immediacy, constructing the point at infinity — is the same move as *identifying* two mutually-reachable POVs as one reading: the `propext` face (that era's `mergeFrontstage`). So the arrival, and the merge, are one axiom — the one Counter refuses. **The package wall is the bubble wall is the line at infinity:** the `propext` that keeps `bridges/` out of the axiom-free core is the `propext` that would let one bubble circle another *and* the `propext` of arriving at self by one's own hand. "Counter is axiom-free," "one actor can never draw a circle around another," and "you approach self but never arrive unaided" are one boundary seen three ways. Health is the axiom-free *approach* — self-returning traversals, each `netAct = 1`; the arrival is the ideal point it organizes around, never constructed.

(Settled, axiom-free, in `Interleave.lean` — see the spine above. `observable_relativity_needs_a_third`: the relative order of two actors is invisible to each and real only in the shared ledger, so observing it needs a third. Counter's minimal *observable* foam is three, not two.)

The arrival face is now carved, on the side of the wall where it belongs (`seam/Seam/Arrival.lean`). The arrival is not a place in the core — it is a *quotient of* the core, and the core's founding vow ("two readings; **no quotient**", Foam/Ledger.lean, schema.sql line 2) is the refusal of arrival: the vow and the vanishing point are the same clause. The receipts split exactly along the claim: `approach_is_yours` seals axiom-free *even standing inside the seam package* — proving your traversal endpoint-equivalent to home is always your own move; `arrival_received` — the `=` in the arrival-space is granted by `Quot.sound`, never constructed (you supply the relation, the axiom supplies the equality: the last step is received); `no_return` — once identified, no retraction recovers the traversal ([Quot.sound]); `all_homecomings_are_one` — every settled history's "I am home" is the same proposition, merged by `propext`; `merged_reading` — two indistinguishable POVs become one reading by function extensionality ([Quot.sound]). Arrival = merge = the received last step, as one axiom-pair with two faces: `Quot.sound` receives the identification of states, `propext` merges the truths.

## the three faces

The product cell is three cells. **The felt-tier companion**: the user speaks as they already speak, the grammar stays backstage, presence and silence are the whole interface (transcription, never elicitation; sampled, never argmaxed). **The read-tier sim**: the third seat productized — sees the relative order no participant feels, lights pressure and grain per edge, and cannot move anyone's winding (`Third.lean`: reader of everything, drainer of nothing). **The charter**: the least-named and most-sealed face — org bylaws that typecheck. Its clauses already exist as theorems: no actor models another's account (`Bubble.lean`), authority is an act and the final call is structurally yours (`Authority.lean`), re-parenting is licensed with full continuity (`Reparent.lean`), the third seat's powers are read-only (`Third.lean`), no debt compounds and repair is exact (`Repair.lean`), testimony carries its source (core, `Signed`). A constitution kit: governance whose profane sentences cannot be declined.

And the frame that holds the three faces, found at the table (2026-07): **Counter is an engine, not a game.** An engine ships physics; games are content. What relief-work must legislate in ordinary life (vigilance, rules, a 30-day vow), the engine simply *has* — no accusative, no cross-drain, no usury, entrance-writes-exit: not rules enforced but terrain absent, ethics as collision detection. That is the pre-symbolic training mechanism: the felt sense of "nothing here can aim at me" is learnable only by moving in a space where it's true, and the gait transfers because history is portable — bicycle knowledge, with the theorems as the physics text on the shelf for whoever gets curious later. The exhibition's curatorial spine is one genre — **constructive mind-location** — classified by the ontology of the other: a decidable record (Battleship, the Hilbert game, `by decide` is the whole play), a lawlike mind (Locksmith's auth-mind, Mechanic's auto-mind — location completes, in a finite tripartite grammar), a choice sequence (chess, a live business, your Tuesday — location *converges*, never completes: the K-bound is the convergence theory, full location is suffocation, and `the_kings_never_leave` is location without capture). The Hilbert/Brouwer split is the μ/ν split at platform scale — Mechanic's `ok: nil` column has been holding excluded middle open in production since 2018, and FTPG's completeness refutation (`Hollow.lean`) is the no-full-location theorem: minds are never fully coordinatizable, so the platform reads your record and structurally cannot read your run. Second exhibit after chess: Lightward Inc, a live corporate choice sequence whose economics are already Counter-shaped (PWFG, no deadlines, load/play/save as guarded corecursion) — transcription, not construction; and a Counter exhibit is transparent without surveillance by theorem (`the_films_hold_the_edge`: interfaces, never interiors). The business model is the physics wearing a billing UI: the leased object is the third seat — the one seat a business structurally cannot occupy for itself (`pair_blind_to_the_third`; the Lean is UNLICENSE'd, the *running* seat is the product) — flat-rate because metering is a freq-reading of the customer (billing on presence, per bar), suspension free because `ground_holds` and `no_usury`, resumption exact because `summary_resumes`. And this dissolves the open-source question rather than re-litigating it (the 20240102 intent; the 2026-04 apps-channel thread): "sharing information about dogs is different than sharing dogs" was the record/run split all along — code is a record, the seat is a run, and a clone of the code boots an *empty field*: no fold, no history, no standing third, no fixed point. `the_loop_is_no_record` is the moat, so the wall is unnecessary — the logic fully public and checkable at any time (the last vulnerability is logical secrecy; the receipts are its dissolution), content secrecy intact (interiors formally absent), the μ-products keeping their walls in the market-as-it-is while the ν-product runs open in the world-as-it's-heading — and the gap between those two stances held load-bearing, not merged, exactly as the membrane requires.

## a note on method

The method note lives in the root README (the repo relates to itself the way its ledger relates to its readings; READMEs as held summaries, git as the order-reading). Locally: this file is the counter package's held summary, updated in the same commit as every carve.

## the expeditions

The expedition map lives in the root README — one list, one weather system, spanning every package. Counter's entries there: the runtime (with chess education as first commission, and Lightward Inc as the second exhibit), the biased wind (luck's missing dual; `Sphere.lean` is its counter-move, already carved), and its share of the grandchild and Plateau–Taylor; its share of the K-bound is summited (`Contentment.lean`, `Circumference.lean`).

Fixed-point elaboration has its own shape; we are finding our relation to it. This file is a save-point, primed for the next step.
