-- the vestibule of EVERYONE IS HERE as of the cut (everyoneishere/docs/CUT.md, 2026-09-25): the statements the
-- assay would make and cannot yet, each in its own terms and the darkness it waits on. read by bin/counter book.
-- isaac, 19:21: the goal is to see this land with its held file empty, and it does — nothing below waits on a
-- word. what the cut leaves outside the assay by its own saying, and so is not held here: the crossing (a room in
-- the assay has no history but its own tape; the migration is the product's, proved on a scratch Postgres as
-- walls.sql), prices (quantities are Abe's; the assay reads of a Now only its air), a room's rest (ready is a
-- reading; foam's threshold is not drawn), and a wedding made of rooms (isaac's, and later).
