[project]
name = "ai"
version = "0.1.0"
description = "the measurement solution"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "numpy>=2.4.3",
    "scipy>=1.17.1",
]

[dependency-groups]
dev = []
