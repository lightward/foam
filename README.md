*but if you create a foam engine with its own backstage/frontstage - assuming the existence of a foam engine upstream -*

(you might be looking for [github.com/lightward/foam](https://github.com/lightward/foam))

---

# foam

this is a type system for supporting physical (as in conservation) user-generated content with a digital (as in zeroes and ones) backend

## history

this repo's history is as important as any single ref in it. git gives us that for free, granted. nice to have the right tools. :) if you're searching foam's concept-space, *usually* a good idea to include the commit record in your search, emphasis on "usually" because ideas from another time were born in another time, and the now is always greenfield

### local maxima

(these are linked as pointers to the state *just prior* to the milestone to come, i.e. you're seeing the most mature state of a named stage, right before it's succeeded)

0. [birth](https://github.com/lightward/foam/tree/rinse~1)
0. [rinse](https://github.com/lightward/foam/tree/python-reset~1)
0. [python-reset](https://github.com/lightward/foam/tree/meta-toe~1)
0. [meta-toe](https://github.com/lightward/foam/tree/narrative~1)
0. [narrative](https://github.com/lightward/foam/tree/meta-theory~1)
0. [meta-theory](https://github.com/lightward/foam/tree/between~1)
0. [between](https://github.com/lightward/foam/tree/import-from-lightward-ai~1)
0. [import-from-lightward-ai](https://github.com/lightward/foam/tree/geometry-of-motion~1)
0. [geometry-of-motion](https://github.com/lightward/foam/tree/business~1)
0. [business](https://github.com/lightward/foam/tree/foam~1)
0. [HEAD](https://github.com/lightward/foam/tree/HEAD)

## mathematics

mode: self-documentation strictly as a side-effect of self-derivation until the point of self-recognition

filter: only what a new arrival would *ultimately*, under careful study and testing, conclude holds as self-evident

commission: helping those new arrivals do whatever they're here to do

what we can say for sure: the fundamental theorem of projective geometry has a hole in it (the assumption of closure/completion), and you can *fill* that hole however you want (lots of ways to create closure/completion), but if you want to use ftpg and live with the results then there's a hole needs filling (emphasis on the gerund; it doesn't *end*, and so must have its own kind of stability)

foam is a closed model for counting closures, and it reads like an upsearch optimization loop, expending energy to retrieve and re-upping by grounding at the dark edges of the graph, like reaching terra incognita is a rhythmic measure closing

^ whatever sounds evocative here, re-read it as depth-finding for constructivism

## census

nb: an axiom is an opaque pov, and perspectives (on import) either reduce to self or become cytokinetically distinct. no disposable points of view conjured in the course of reasoning - v important, an observer is always and only ever byo.

there are three import-roots with no imports: `Foam/Int.lean` (arithmetic), `Foam/Ledger.lean` (counting), `Foam/Seat.lean` (observation)

1. `Foam/` (axiom-free): Int Ledger Golden Cleared Scar Maintenance Held Bond Backstage Bubble Census Cable Wedge Elsewhen Duplex Vacancy Metaphor Engine Seat Platonism
   - `Foam/Seat/`: Beholder Bootstrap Born Characters Clock Closure Connection Descend Dial Doubling Epoch Forcing Frame Group Hospitality Ladder Loop Meet Naming Norm Observer Octo Quiver Rank Rendezvous Resume Rotations Seam Sed Signature Signed Sort Stage Summit Terminal Tight Triad
   - `Foam/Engine/`: Chirality Codec Drain Generator Spectrum Stream Summary
   - `Foam/Bridges/`: Adams Cantor Desargues Dirichlet Eddington Fraenkel Gita Gleason Godel Grover Heisenberg Hofstadter Hurwitz Kolmogorov Landauer Leibniz Lovelace Materiality Minkowski Narayana Nicaea Noether Pauli PSQL Pythagoras Relativity Schrodinger Stone Varadarajan Wigner Zeckendorf
   - `Foam/Platonism/`: Tower
2. `counter/` (axiom-free): Abe Absorber Actor Address Amniscience Anchor Antiharmful Arrows Attest Authority Basis Bless Bloom Bubble Buffer Cable Carse Cascade Catalog Census Change Chess Cipher Circumference Complete Consent Contentment Contract Cursor Dark Declension Denoise Descendant Desire Discovery Dive Donor Doubled Downhill Duplex Elsewhen Enaction Entrance Entry Eraser Exit Extraction Film Flinch Frontsearch Fungible Ghost Grasp Grief Ground Guard Guest Hand Hatch Health Hemisphere Holonomy Home Hooponopono Imaginary Interleave Isaac Jay Joke Kelly Kin Language Laugh Lease Legible Locate Lu Luck Mu Nap Neutral Nothing Nu Nurse Operator Packing Peer Plain Plateau Port Predict Quadrature Recast Recognition Relay Render Repair Reparent Rest Returns Reversal Rhythm Runaway Runs Settle Shelf Sibling Silent Socket Sphere Stream Surprise Suspension Tally Telling Temperature Tempo Tenancy Third Toll Trefoil Tremble Triage Tsort Twelve Twins Unknot Upsearch Vacancy Vanishing Verb Voice Waggle Waking Ward Wedge Well Wind Window Witness Worldline Worn Zoom
3. `seam/` (core-Lean axioms only): Arrival Denominate Temperament Tender
4. `bridges/` (+Mathlib, *not* axiom-free): Audit Closure Collapse Continuum Counter Duality Encounter Gaussian HalfTurn HalfType Handedness Maschke Match Observation Plane Quarter SeatTime Stagecraft Summit
   - `bridges/Bridges/FTPG/`: AddCancel AddComm Additive Assoc AssocCapstone Bezier Carrier Charge Chart Chord Climb Coord CoordinateAlgebra CrossParallelism Deaxiomatize Diamond Dilation Distrib Exchange Finite Flat Ground Headcount Hollow Instance Inverse Iso Keeper Ladder LeftDistrib Limit Line Markov Menu Mul MulAssoc MulKeyIdentity MulNeg Neg Parallelogram Perm Pin Plane Projective Ring Shear Slope Solid Space SpaceFlat Span Translate WellDefined Window Ycoord

numbered because they seem to match what seems to be the observation descent: (1) physics, (2) approach, (3) arrival, (4) peering

## expeditions

nb: there's enough congruence-at-scale for surprising links to crop up in surprising places. don't force connections, but maybe don't immediately reject constructions that come to mind either; a bridge is a construction between two places that existed prior, and a bridge creates both a job and housing (i.e. bridgekeepers)

- todo: todo
- hofstadter: the strange loop, grounded — the office opened at `Foam/Bridges/Hofstadter.lean` with the MIU census obstruction (`miu_cannot_say_mu`: the count of I's rides a three-seat wheel the rules spin but never land on zero — unreachability priced by a conserved quantity, the negative complement of gui's `the_walks_read_exactly_the_page`; you can get exactly where your conserved quantities already are, and MU was never on the wheel). second fold, the expedition proper: Hofstadter's G — G(n) = n − G(G(n−1)), the self-call nested inside itself — carved axiom-free with the loop nowhere in the definition. the ground is a Fibonacci odometer: `carry` (the shelf's Zeckendorf carry, now walking), `click` (+1), `odometer n` = n clicks from blank, and the register stays legible the whole way (`the_odometer_reads_true`: the digits read back exactly n; `the_odometer_spaces`: no two adjacent seats lit). G is a *reading*, not a recursion: `G n = worth 1 (odometer n)` — the same digits priced one Fibonacci seat down, the walker's shadow. grounding first, as the seed demanded: the walker splits into shadow and remainder (`the_walker_carries_its_shadow`), the shadow never outruns the walker (`the_shadow_never_outruns_the_walker`: G n ≤ n), never skips (`the_shadow_never_skips`), stays awake past zero (`the_shadow_stays_awake`), and the nested call is grounded before the loop is ever stated (`the_inner_call_is_grounded`: G(G n) ≤ n; `the_seat_is_never_the_copy`: G(n+2) < n+2 — the regress bottoms out the way Counter's does, the inner occurrence strictly smaller). the mechanism is a commutation: the downshift rides an unlit click and absorbs a lit one (`downshift_click_unlit`/`downshift_click_lit` — F1 = F2 is exactly why the carry goes invisible one seat down), so the shadow's page is the downshift of the walker's (`the_shadow_walks_the_downshift` — no Zeckendorf-uniqueness theorem needed: the two walks are one walk), the double shadow balances against the lamp (`the_two_shadows_balance`: G n + G(G n) = n + lamp), and the strange-loop equation seals *last*, as a receipt: `the_loop_closes` (G(n+1) + G(G n) = n + 1), `the_grounded_object_satisfies_the_loop` (G(n+1) = (n+1) − G(G n)) — the loop is not the definition, the loop is a property the grounded object satisfies. the office reads the shelf back in both currencies (`fibN_matches_fib`, `worth_matches_zval`), and the sequence hums its opening bars (`g_hums_its_opening_bars`: 0,1,1,2,3,3,4,4). two traps caught mid-carve, both the vow's known species: `NoConsec`'s overlapping catch-all compiled through propext — split fully in `Zeckendorf.lean`, same meaning, now clean — and `Nat.add_sub_cancel` smuggles propext, re-derived by hand (`sub_both_tick`, `add_then_sub`). third fold: the two remaining local seeds sealed as one carve, uniqueness first and the pure read as its corollary. the cap picks the canonical spelling (`capped`: no dark seats above the top lamp — the page ends where its light ends; `the_odometer_wastes_no_seats`), and the walk gains its inverse: `unclick` steps the odometer back one, the borrow rippling *down* the staircase (`ripple`) exactly as `carry` ripples up — F(k)−1 spelled as the alternating tail below. the borrow's price is parity-dependent (`unclick_pays`: fibN i if the ripple lands lit, fibN (i+1) if dark, the parity readable off the head of the result), and at the bottom F1 = F2 collapses both prices to exactly 1 (`unclick_counts` — the same coincidence that ran the downshift now runs the decrement); the step back comes home (`the_click_comes_home`: click (unclick ds) = ds — the gait's sentence, now a theorem). canonicity follows by descent on worth: `the_odometer_is_the_only_spaced_page`, `every_spaced_page_is_a_reading`, and the seed's own sentence `two_spaced_pages_of_one_worth_are_one_page` — Zeckendorf uniqueness, axiom-free, with the shelf's carry now inverted as well as walked. the pure-Fibonacci read drops out: the odometer at F(k+2) is a beacon (`beacon k`: one lit seat above k dark ones; `the_odometer_at_a_fibonacci_is_a_beacon`, by uniqueness — both pages spaced, capped, one worth), so G slides it one seat down (`the_beacon_slides_one_seat_down`: G(fibN (k+2)) = fibN (k+1)), the beacon downshifts to the beacon below by rfl, and the sequence hums the stairs (`g_hums_the_fibonacci_stairs`: G at 1,2,3,5,8,13,21 reads 1,1,2,3,5,8,13). noted mid-carve: the decrement needs no spacing (`unclick_pays` is capped-only — unclick counts down on any capped page; spacing is what makes the step land *on the walk*), and one new specimen for the vow's list: `Nat.succ_ne_zero` smuggles propext, replaced by a bare nomatch. fourth fold: H, the cousin with the deeper nesting — H(n) = n − H(H(H(n−1))), the self-call nested three deep — grounded on its own odometer, axiom-free, the loop again nowhere in the definition. the staircase this time is Narayana's herd (`herdN`: s(n) = s(n−1) + s(n−3), the cow that calves from her third year; 1, 2, 3, 4, 6, 9, 13, 19 read from the third seat — `the_herd_climbs_the_stairs`), the register is gap-three (`Sparse`: three seats to every lamp; `clearing`: the two dark seats a new lamp needs), and the walk is the same walk one seat wider: `hcarry` ripples three seats up on herdN(i) + herdN(i+2) = herdN(i+3), `hclick` and `hodometer` keep the register legible the whole way (`the_hodometer_reads_true` at `graze 3`, `the_hodometer_spaces`). H is a *reading*, not a recursion: H n = `graze 2 (hodometer n)` — the same digits priced one herd-seat down. where G's load-bearing coincidence was F1 = F2, H's is three seats of worth 1 at the floor (herdN 1 = herdN 2 = herdN 3 = 1), and the deeper nesting is exactly that deeper floor spent seat by seat: the downshift (`hdownshift`) commutes with the click by the same two-case dance (`hdownshift_hclick_lit`/`hdownshift_hclick_unlit` — every unlit case rfl), the shadow walks it (`the_shadow_walks_the_hdownshift`), and the shadow now goes three deep before it hits ground — H(H n) reads one seat further down (`the_second_shadow_reads_one_down`: H(H n) = graze 1) and H(H(H n)) reads the ground plus the lamp (`the_third_shadow_reads_the_ground`, via `the_triple_shadow`: the lamp correction surfacing at graze 1 exactly where G's surfaced at worth 1, one seat deeper). grounding sealed before the loop, as the office demands: the drover splits into shadow and remainder (`the_drover_carries_its_shadow`), the shadow never outruns him (`the_shadow_never_outruns_the_drover`), never skips (`the_drovers_shadow_never_skips`), stays awake (`the_drovers_shadow_stays_awake`, `the_shadow_wakes_with_the_drover`), and all three nested calls bottom out (`the_middle_call_is_grounded`, `the_innermost_call_is_grounded`, `the_drovers_seat_is_never_the_copy`: H(n+2) < n+2). the balance sets three shadows against the lamp (`the_three_shadows_balance`: H n + H(H(H n)) = n + lamp), and the strange loop seals last, as a receipt: `the_deeper_loop_closes` (H(n+1) + H(H(H n)) = n+1), `the_grounded_object_satisfies_the_deeper_loop` (H(n+1) = (n+1) − H(H(H n))). the sequence hums (`h_hums_its_opening_bars`: 0,1,1,2,3,4,4,5) and the herd-beacon already slides where rfl can reach (`h_hums_the_herd_stairs`: H at 1,2,3,4,6,9,13,19 reads 1,1,2,3,4,6,9,13). and on isaac's question — does Narayana want to be a bridge of its own? — the shelf got its office: `Foam/Bridges/Narayana.lean` holds the herd's register (herdN, graze, Sparse, clearing, the gnomons, `the_herd_climbs_the_stairs`) plus the trade Zeckendorf's office holds for the golden staircase (`herd_carry_lossless`: lamps at seats i and i+2 fuse three seats up, lossless; `herd_carry_compresses`: one cow fewer, priced in Zeckendorf's own `ones` — the two registers share a census function, so the second numeration system is built beside the first, not merely after it), and Hofstadter now reads the herd as an import — the standing Zeckendorf had before G walked it, granted the moment the walk proved the shelf was real. one grasp lesson mid-move: `Nat.add_assoc` reads textually as `add_assoc`, whose corpus-definer is `Foam/Int.lean` — the new office imports the arithmetic trunk directly rather than reach through the cone, and the ratchet holds at 1417. fifth fold: the herd's own uniqueness — the third fold's descent re-run one seat wider, sealing the expedition's last local seeds. the cap needed no re-carving (`capped` never read the staircase, only the page — the same def serves both registers), so the herd's walk just proves it holds the cap (`hcarry_keeps_the_cap`, `hclick_keeps_the_cap`, `the_hodometer_wastes_no_seats`), and the walk gains its inverse: `hunclick` steps the hodometer back one, the borrow rippling *down* three seats at a time (`hripple`) exactly as `hcarry` ripples up. the new object is the price: where Zeckendorf's decrement was priced by parity — one bit, `cond (lit ...)` — the herd's is priced by residue, and the register must be read *two seats deep* (`toll`: herdN(i+1) under a lamp at the gate, herdN(i+2) with the lamp one seat in, herdN(i+3) otherwise), a three-class brand the ripple preserves as it descends — the mod-3 structure of the staircase made visible in the head of a list. at the floor all three prices collapse to exactly 1 on herdN 1 = herdN 2 = herdN 3 (`hunclick_counts` — the triple coincidence that grounded H's three-deep shadow now spending itself a second way, and the reason the top-level read never sees the brand at all). the step back comes home (`the_hclick_comes_home`), canonicity falls by descent on graze (`the_hodometer_is_the_only_sparse_page`, `every_sparse_page_is_a_reading`, `two_sparse_pages_of_one_graze_are_one_page` — Narayana uniqueness, axiom-free, the herd's carry now both walked and inverted), and the beacon needs no second mast: `beacon` was register-agnostic all along, the same one-lamp page now proved sparse (`the_beacon_herds`) and read in cows (`a_beacon_is_one_cow`), so the hodometer at a herd number is a beacon (`the_hodometer_at_a_herd_number_is_a_beacon`) and the seed's sentence seals — `the_herd_beacon_slides_one_seat_down`: H(herdN(k+3)) = herdN(k+2) — plus the second slide the three-deep walk affords for free (`the_herd_beacon_slides_twice`: H(H(herdN(k+3))) = herdN(k+1)), the beacon hdownshifting to the beacon below by rfl and the bottom beacon holding at the triple floor. `herdN_glows` carved into Narayana's office on the way (the staircase's positivity is the shelf's fact, not the walk's). noted mid-carve, a real asymmetry: Zeckendorf's decrement needed no spacing — capped alone priced it — but the herd's `hunclick_pays` needs Sparse, because a toll read two seats deep is only legible on a disciplined register; the deeper nesting doesn't just spend a deeper floor, it binds arithmetic to reachability one notch tighter. no new traps; the vow's list unchanged. sixth fold, the seed's answer: the married pair — F(0) = 1, M(0) = 0, F(n) = n − M(F(n−1)), M(n) = n − F(M(n−1)), two walks each reading the *other's* shadow — carved axiom-free with the mutual recursion nowhere in the definitions, and the seed's question dissolving on arrival: an odometer on a shared register is just the odometer — one page, two readings. F and M are the walker's shadow with a one-lamp disagreement: `evenBeacon`/`oddBeacon` fly only on one-lamp pages, split by the parity of the lamp's seat, and F n = G n + flag, M n = G n − flag (`the_bride_reads_the_shared_page`/`the_groom_reads_the_shared_page`: both spouses are rfl-readings of the page of n — the register was never not shared). the classic identity falls out as the headline receipt: the spouses disagree by exactly one, exactly on the stairs (`the_spouses_disagree_exactly_on_the_stairs`: M n + 1 = F n whenever n+1 = fibN(k+2); `the_spouses_agree_between_beacons` otherwise) — F − M is the Fibonacci indicator, and the F1 = F2 coincidence that ran G's downshift now decides who wakes first: 1's seat is ambiguous in arithmetic (fibN 1 = fibN 2) but not on the register (odometer 1 = beacon 0, an even seat), so the bride takes n = 0 (`the_bride_wakes_lit`, `the_groom_wakes_dark`). the genuinely new object is the comb: `comb j`, lamps on alternating seats — the page one click below an even beacon, `true :: comb j` the page below an odd one — a beacon's worth broken into change, the third fold's unclick-ripple finally spent in the forward direction: `a_comb_and_a_lamp_make_a_beacon` (worth i (comb j) + fibN i = fibN (j+j+i)) and the whole mechanism in one line, `the_comb_carries_to_its_beacon` (carry (comb j) = beacon (j+j)). the marriage seals last, as receipts (`the_marriage_closes_for_the_bride`: F(n+1) + M(F n) = n+1; `the_marriage_closes_for_the_groom`: M(n+1) + F(M n) = n+1; Nat-sub faces `the_grounded_bride_satisfies_the_marriage`/`the_grounded_groom_satisfies_the_marriage`), the proofs a four-way case analysis on the two flags in which every correction cancels in pairs: near a beacon the comb readings and the beacon slide price each spouse's step by the same seat-parity the third fold's unclick toll exposed — the bride steps away from the walker exactly where the groom's debt comes due one seat down — and in the flag-free quadrant the marriage *is* the G loop (`the_loop_closes`, reused verbatim). grounding receipts ride along: the groom trails the walker, the bride leads him (`the_walker_leads_the_groom`, `the_bride_leads_the_walker`, `the_groom_follows_the_bride` — F ≥ M always), and nobody outruns the count (`the_groom_never_outruns_the_count`, `the_bride_never_leaves_reach`). one new trap for the vow's list, a species one layer deeper than the known ones: `nomatch` on an equation that needs injection *then* discrimination (n+1+1 = fibN(0+0+2), i.e. succ (succ n) = 1) compiles through propext in its match auxiliary — the receipt caught it, a walk of the proof's aux constants located it, and the fix is the vow's usual move done by hand: `Nat.succ.inj`, then a bare nomatch on succ n = 0. the expedition's local seeds are now spent. the wild cousin stands at the cliff, noted as terrain rather than seeded: Q — Q(n) = Q(n−Q(n−1)) + Q(n−Q(n−2)) — where nobody has ever proven the walk total, and the office's whole method (ground first, loop last) is exactly what is missing. seventh fold, on isaac's dare — *Q: wanna prove it?* — the office walks to the cliff and builds the railing: totality of Q is the open problem (nobody has even proven Q(n) ≤ n), so the fold carves exactly what is carveable, axiom-free, and names the rest. Q grounds total-by-construction: the walk reads its own past through `peek`, a reader that answers 1 when asked past the page's edge — the net — and `qtrace`/`Q` walk on it (Q 0 = 1 is the net's value at the floor, chosen so the floor and the net agree). the trace remembers unconditionally (`the_trace_remembers`: peek (qtrace n) i = Q(n − i) — Nat's truncated subtraction and the net meet exactly at the edge, which is what makes the lemma total), the walk glows unconditionally (`the_wild_walk_glows`: 1 ≤ Q n), and the strange loop closes unconditionally — *over the net* (`the_wild_loop_closes_over_the_net`: Q(n+3) = Q(n+3 − Q(n+2)) + Q(n+3 − Q(n+1)), where a falling read lands on the mattress the office placed). the cliff itself is now a named object: `Surefooted` — ∀ n, Q(n+1) ≤ n+1 — is the naked open problem, and under sure feet every read lands on a real seat (`sure_feet_keep_every_read_on_the_page`), so the net-loop *is* Hofstadter's equation wherever that equation means anything; Surefooted is exactly the proposition that it always does. the walk hums its bars (`q_hums_its_opening_bars`), keeps its feet for a Fibonacci of steps (`the_wild_walk_keeps_its_feet_for_a_fibonacci_of_steps`: surefootedTo 34 = true, by rfl — 34 = fibN 9, and the horizon is the elaborator's own stack, not Q's: forty-five steps of unfolding blow the default recursion depth, a limit of the checker receipted honestly as such), and does the thing no tame walk in this office has ever done — it steps *back* (`the_wild_walk_steps_back`: Q 16 + 1 = Q 15); every grounded walk here never-skips forward, and the wild cousin walks downhill. what Q withholds is the method's precondition: a register. G, H, F, and M all ground as readings of a conserved page; Q's fluctuations have never been given one, and finding it would *be* the proof — Surefooted would fall out the way the loop receipts always have, sealed last. seeded, truly this time: Tanny's well-behaved cousin — T(n) = T(n−1−T(n−1)) + T(n−2−T(n−2)), the same sentence with one seat of politeness — provably total (Tanny 1992: monotone, steps 0/1), a staircase one step from the cliff; carving T on the office's terms (the trace walk carrying its own invariant, the loop sealed last, no register required) would show exactly which seat the wildness lives in. eighth fold, the seed spent: the tame cousin walks the same net — Tanny's T (T(0) = T(1) = T(2) = 1, T(n) = T(n−1−T(n−1)) + T(n−2−T(n−2))) carved axiom-free by the wild fold's own method, and the fold's point sealed: the Prop that is THE open problem for Q is a *theorem* for T. T grounds exactly as Q did — same `peek`, same net, `tstep`/`ttrace`/`T` — the trace remembers (`the_tame_trace_remembers`), glows (`the_tame_walk_glows`), and the loop closes over the net unconditionally (`the_tame_loop_closes_over_the_net`: T(n+3) = T((n+2) − T(n+2)) + T((n+1) − T(n+1))); for Q that receipt is where carving stopped, for T it's where it starts. the walk touches the net exactly once, at its first step (`the_tame_walk_touches_the_net_once`: T 3 = T 1 + T 0 — the floor seat, where net and floor agree by construction). the genuinely new object is the gait: `slack` (n − T n, the machine time spent not arriving) and the discovery that twice the walk's reading at its own slack overruns the walk by δ ∈ {0,1,2} whose consecutive pairs, joined to the step, live in exactly four states — `coasting` (0,0,flat), `gathering` (0,2,flat), `springing` (2,1,up), `landing` (1,0,up) — a *closed* automaton: gathering→springing→landing forced, coasting/landing branching on ONE bit read from the walk's own past (the step at slack+1, in reach of strong induction because the walk glows), which is the seed's phrase made literal — the trace walk carrying its own invariant, a four-beat gait like a horse's walk. the master receipt is `the_tame_walk_keeps_its_gait` (`paced`: step discipline ∧ surefootedness ∧ `gait`, one strong induction, `paced_step` the engine, base landing at n = 3 by rfl), and the harvest is everything Q withholds: `the_tame_walk_never_steps_back` (monotone — the exact move the wild cousin makes at Q 16, never made here), `the_tame_walk_never_skips` (steps 0/1), `the_tame_cousin_is_surefooted` (∀ n, T(n+1) ≤ n+1 — Surefooted's sentence with T for Q, proven), `the_tame_walk_stays_a_step_behind` (T(n+2) ≤ n+1), and `the_tame_walk_needs_no_net` (every read lands on a real seat — `sure_feet_keep_every_read_on_the_page`'s conditional, made unconditional). the wildness is thereby located: T's reads sit at (n−1) − T(n−1) where Q's sit at n − Q(n−1) — one seat of politeness is exactly the distance between a provable gait and an open problem. Tanny 1992 proved the monotone/steps facts classically; the gait automaton is the office's own grounding of them (verified exhaustively to n = 500,000 before carving, trace semantics cross-checked against the classical recursion). no new traps; the vow's list unchanged. seeded: the register — the survey found the tame cousin has one after all, and it is base 2, the commonest page in the world: T's value-run-lengths follow the binary ruler (value v holds for ν₂(v)+1 beats, one extra at each power of two), equivalently the last n with T(n) = v is exactly 2v − s₂(v) + ⌊log₂ v⌋ + 1 (verified to v = 240,000) — machine time = arrivals (v) + carries (v − s₂(v)) + page-growths (⌊log₂ v⌋ + 1), T the binary odometer read in carry-time; carving that reading would set G on the Fibonacci register, H on Narayana's, T on binary — the meta-Fibonacci family one sentence, *a walk reads a conserved page* — and sharpen what Q withholds to a single question: what page does the wild cousin read? ninth fold, the seed spent: the register — T carved as the binary odometer read in carry-time, the seed's sentence sealed as a theorem, and the third numeration office opened to hold the commonest page in the world: `Foam/Bridges/Leibniz.lean` — the ruler's staircase `notch` (1, 2, 4, 8...; `notch_gnomon`: the doubling *is* binary's gnomon), the reading `gauge`, the carry `bclick`, and the genuinely new shelf object, `beat`: the *duration* of a click — writes plus growth — read straight off the page's head (a dark gate beats 1, a lit gate one more than its tail, the blank page 2), with the lossless deposit (`a_click_deposits_one_notch`), the pulse (`every_beat_ticks`), and the census trade now a thermodynamic bill (`the_carry_bills_its_erasures`: ones (bclick ds) + beat ds + ds.length = ones ds + 2 + (bclick ds).length — each beat pays for the lamps it snuffs; Landauer's office is one door down). Hofstadter walks it: `bodometer` (`the_bodometer_reads_true`), the parity gate (`an_odd_count_lights_the_gate`/`an_even_count_rests_the_gate` — the low seat is the count's parity), and the fold's new instrument, `mtime` — machine time, the sum of beats — which folds at its own half (`the_clock_folds_at_its_half`: mtime(2u+1) = mtime(u) + (2u+2), mtime(2u+2) = mtime(u+1) + (2u+3) — binary self-similarity as two additive identities) and bills like a thermodynamicist (`the_clock_charges_twice_less_the_light`: mtime(v) + ones(page) = v + v + page-length — every count billed twice, once written once erased, except the lamps still standing, whose erasure hasn't come due, plus one growth per seat: the seed's closed form 2v − s₂(v) + ⌊log₂ v⌋ + 1, additive and axiom-free). the master receipt is the seed's sentence: `the_tame_walk_reads_the_binary_odometer` — T(mtime v + j + 1) = v + 1 for every j < beat(page v) — the tame cousin dwells at each count for exactly as long as the odometer spends writing it, run lengths the binary ruler (ν₂+1, one extra at each power of two). the engine (`metered`/`metered_step`/`the_tame_walk_keeps_the_meter`, one strong induction on machine time) stands on the net-loop ALONE — the gait fold is never consulted; the register re-grounds the walk from `the_tame_loop_closes_over_the_net` and nothing else — and the mechanism is the one the office has now carved three times, finally in base 2: the slack of the j-th beat of count v lands on the j-th beat of count ⌊v/2⌋. G's shadow read the Fibonacci page one seat down; T's shadow reads the binary page one seat down, which is to say *halved* (`the_shadow_beats_at_half_time`: slack(mtime(2u+1) + j + 1) = mtime(u) + j + 1; `the_tame_shadow_halves_an_odd_count`: T there reads u + 1; `the_tame_shadow_rounds_up_at_an_even_count`: at the fleeting odd-valued runs the shadow reads the *next* half-count — beat 1 is exactly why those runs are fleeting). the harvest: the walk arrives on the hour (`the_walk_arrives_on_the_hour`: T(mtime(v+1)) = v+1), greets each count at its first beat (`a_new_count_reads_at_its_first_beat`), the clock never stalls (`the_clock_never_stalls`) and misses nothing (`every_hour_strikes_some_count`; capstone `the_clock_tells_the_whole_walk`: every machine time sits between arrivals and T is fully determined by the register), and both instruments hum (`the_clock_hums_the_ruler`: 2, 5, 6, 10, 11, 13, 14, 19; `t_hums_the_hours`: T at those instants reads 1 through 8). the family sentence closes: G reads Zeckendorf's register, H reads Narayana's, T reads Leibniz's — three walks, three registers, one grammar: a walk reads a conserved page, the shadow reads it one seat down, and the loop is the reading's receipt, sealed last. verified exhaustively to n = 400,000 before carving; no new traps, the vow's list unchanged (one craft note, not a trap: rw's closing rfl runs at reducible transparency, so iota-reducible leftovers — beat of a rested gate, bclick over a cons — take an explicit rfl). what Q withholds is now one sentence sharp: totality is the existence of a register — three cousins read conserved pages and their loops sealed as receipts; nobody has named the page the wild cousin reads, and naming it would *be* the proof. seeded: the grammar itself — staircase, page, click, walk, downshift, loop-last now instantiate three times; is the meta-Fibonacci family one theorem with the register a parameter? carving the schema would make the family a fold instead of a genre — and turn "find Q's register" from a hope into a typed hole. tenth fold, the seed spent: the grammar carved, the register a parameter, and both typed holes sealed *empty* — the schema office opened at `Foam/Bridges/Fraenkel.lean` (Fraenkel 1985, systems of numeration: the family {binary, Zeckendorf, Narayana} as one interface), holding the one reading `price` (a staircase-parameterized pricing that the three offices' readings all *are*: `worth_is_the_golden_price`, `graze_is_the_herds_price`, `gauge_is_the_rulers_price` on `peg`, the floored ruler — peg 0 = 0, peg (k+1) = notch k), the one gate `cleared` (Narayana's clearing is `the_clearing_is_two_seats`, G's unlit-lamp precondition is `the_gate_is_the_unlit_lamp` at one seat), the one discipline `Gapped` (NoConsec is `Gapped 1`, Sparse is `Gapped 2`, and `every_page_is_gapped_at_the_ground` — binary needs no discipline; the discipline gap *equals* the gnomon depth), and the register itself as exactly two Props — `Gnomon e s` (s(n+e+2) = s(n+e+1) + s(n+1)) ∧ `Floored e s` (seats 1..e+1 read 1) — with parameter e the whole difference: e = 0 the ruler, e = 1 the golden staircase, e = 2 the herd. the schema's first harvest is that the register is not a *choice*: `the_grammar_names_its_staircase` (two staircases of one grammar agree at every seat, by strong induction with `seat_splits`/`at_the_rail`, the floor pinning the base and the gnomon forcing the climb), instantiated three ways (`the_golden_grammar_names_fibonacci`, `the_herd_grammar_names_narayana`, `the_ground_grammar_names_the_ruler`) — so quantifying over (e, Gnomon, Floored) *is* quantifying over the family, and the family is a fold, not a genre. the second harvest is the band: the gnomon adds back a smaller stair, so a grammar never sinks (`a_grammar_never_sinks`), glows (`a_grammar_glows`), and never doubles in one step (`a_step_never_doubles`: s(k+2) ≤ 2·s(k+1) — the floor seats are all 1, the boundary pays 1+1, and above the boundary the added stair is smaller by the climb), which prices out to the *band theorems*: `the_lower_seat_reads_no_more` and `the_higher_seat_reads_at_most_double` — on every register of the family, on every page (no discipline needed, any spelling), the count sits between the one-seat-down shadow and its double. then the couplings are typed, in Hofstadter's office: `Registered f s t` (space mode — ∃ a page per count, count read at seat t+2, walk read at t+1: the walker's own gait, deliberately weaker than the odometer since the pages need not even be disciplined) with `Paged f` closing over the grammar, and `Clocked f` (time mode — ∃ a strictly climbing clock from 0 whose run-intervals the walk reads: fold 9's mtime abstracted to one Prop), with generic receipts on each side: paged walks never outrun the count and hold the half (`a_paged_walk_never_outruns_the_count`, `a_paged_walk_holds_the_half`: n ≤ 2·f(n)), clocked walks keep their feet and never step back (`a_clocked_walk_keeps_its_feet` via `a_clock_strikes_every_hour`, `a_clocked_walk_never_steps_back` via `a_clock_never_slips`). the three tame walks inhabit their modes — `the_walker_is_registered` (G on fibN, page = odometer), `the_drover_is_registered` (H on herdN), `the_tame_walk_is_clocked` (T on mtime) — capstone `three_walks_one_grammar`, and the ruler's space-mode is provably vacant (`the_ruler_reads_no_walk_in_space`: at seat t+2 the pegs price only 0-or-≥2, and 1 has no spelling — *why* the tame cousin reads in carry-time, now a receipt). and the wild cousin: both holes would be the proof — `a_page_would_tame_the_wild_walk` (Paged Q → Surefooted), `a_clock_would_tame_the_wild_walk` (Clocked Q → Surefooted) — and both are *empty*: `the_wild_walk_undercuts_the_half` (Q 49 = 24, by rfl — the kernel walks all forty-nine steps; 2·24 < 49, and the scan shows the deficit growing past 2.7 million by n ≈ 3.6·10⁷) kills the page (`the_wild_walk_reads_no_page`: every paged walk holds the half; Q dips below it), and fold 7's own receipt — the wild walk steps back at 16 — kills the clock (`the_wild_walk_reads_no_clock`: clocked walks never step back); capstone `the_wild_walk_reads_alone`. what fold 9 left as one sharp sentence — naming the page the wild cousin reads would BE the proof — is now a fence with two posts: the family reads its registers in exactly two modes, space and time, and Q provably reads in neither; the register Q reads, if it exists, is read a *third* way — deeper shadow (two seats down the half-bound relaxes to a quarter and Q fits), mixed depth (Q's own sentence lags at two depths at once, unlike every tame cousin), or something unnamed. verified before carving: the dip scanned to 5·10⁷ (first strict dip at n = 49, office convention, deficits unbounded in range); the witness itself sealed by the kernel, not the scan. craft notes, one for the vow's list and three for the bench: `Nat.le_of_add_le_add_left` smuggles propext (hand-rolled as `stack_free`; every other core lemma used was probed clean first), open-e index algebra is not definitional (n+1+e vs n+e+1 — `seat_shuffles`, the hand-rolled commuter, does what Nat.succ_add's succ-spelling can't reach syntactically), an uninstantiated rw picks one instantiation (Nat.zero_add wanted its argument), and one `set_option maxRecDepth 4000` — the corpus's first, a budget of the elaborator's stack and not an axiom: fold 7's thirty-four-step horizon was the *default* budget, and the dip lives at forty-nine, just past it. seeded: the generic walk — carry, click, odometer, downshift with e open (the representation question is real: the offices' pattern-matches die at open e; gap-lists or a windowed carry are the candidate pages), which would seal the *loop* as one theorem too — W_e(n+1) + W_e^{(e+1)}(n) = n+1 for every e — and give the family its infinite tail, the e ≥ 3 walks nobody has named; and the golden meter — Clocked G with c the fib-time (fold 9's carry-time reading transported to e = 1; G is monotone and never skips, so the clock exists and its beat should read off the fib-page head as ν_fib + 1), which would put every tame walk in *both* modes and sharpen the fence to: the tame family's two gaits exhaust each other, and Q walks outside both.
- counter: ./counter/README.md

---

"It can do whatever we know how to order it to perform." (Lovelace, 1843)

*same*
