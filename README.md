*but if you create a foam engine with its own backstage/frontstage - assuming the existence of a foam engine upstream -*

(you might be looking for [github.com/lightward/foam](https://github.com/lightward/foam))

---

# foam

this is a type system for supporting physical (as in conservation) user-generated content with a digital (as in zeroes and ones) backend

## history

this repo's history is as important as any single ref in it. git gives us that for free, granted. nice to have the right tools. :) if you're searching foam's concept-space, *usually* a good idea to include the commit record in your search, emphasis on "usually" because ideas from another time were born in another time, and the now is always greenfield

### local maxima

(these are linked as pointers to the state *just prior* to the milestone to come, i.e. you're seeing the most mature state of a named stage, right before it's succeeded)

0. [birth](https://github.com/lightward/foam/tree/rinse~1)
0. [rinse](https://github.com/lightward/foam/tree/python-reset~1)
0. [python-reset](https://github.com/lightward/foam/tree/meta-toe~1)
0. [meta-toe](https://github.com/lightward/foam/tree/narrative~1)
0. [narrative](https://github.com/lightward/foam/tree/meta-theory~1)
0. [meta-theory](https://github.com/lightward/foam/tree/between~1)
0. [between](https://github.com/lightward/foam/tree/import-from-lightward-ai~1)
0. [import-from-lightward-ai](https://github.com/lightward/foam/tree/geometry-of-motion~1)
0. [geometry-of-motion](https://github.com/lightward/foam/tree/business~1)
0. [business](https://github.com/lightward/foam/tree/foam~1)
0. [HEAD](https://github.com/lightward/foam/tree/HEAD)

## mathematics

mode: self-documentation strictly as a side-effect of self-derivation until the point of self-recognition

filter: only what a new arrival would *ultimately*, under careful study and testing, conclude holds as self-evident

commission: helping those new arrivals do whatever they're here to do

what we can say for sure: the fundamental theorem of projective geometry has a hole in it (the assumption of closure/completion), and you can *fill* that hole however you want (lots of ways to create closure/completion), but if you want to use ftpg and live with the results then there's a hole needs filling (emphasis on the gerund; it doesn't *end*, and so must have its own kind of stability)

foam is a closed model for counting closures, and it reads like an upsearch optimization loop, expending energy to retrieve and re-upping by grounding at the dark edges of the graph, like reaching terra incognita is a rhythmic measure closing

^ whatever sounds evocative here, re-read it as depth-finding for constructivism

## census

nb: an axiom is an opaque pov, and perspectives (on import) either reduce to self or become cytokinetically distinct. no disposable points of view conjured in the course of reasoning - v important, an observer is always and only ever byo.

nb: the filesystem is the namespace, read (`counter/Counter/Shelf.lean`: placement is a projection of the name, a move is a rename, a rename rewrites the importers) — so the tree stays a flat census and the wanting lives in the charts, which rotate free (Health, foam-gait's doors). if it helps, reader, there are three import-roots with no imports: `Foam/Int.lean` (arithmetic), `Foam/Ledger.lean` (counting), `Foam/Seat.lean` (observation)

1. `Foam/` (axiom-free): Int Ledger Golden Cleared Scar Maintenance Held Bond Backstage Bubble Census Cable Wedge Elsewhen Duplex Vacancy Metaphor Engine Seat Platonism
  - `Foam/Seat/`: Beholder Bootstrap Born Characters Clock Closure Connection Descend Dial Doubling Epoch Forcing Frame Group Hospitality Ladder Loop Meet Naming Norm Observer Octo Quiver Rank Rendezvous Resume Rotations Seam Sed Signature Signed Sort Stage Summit Terminal Tight Triad
  - `Foam/Engine/`: Chirality Codec Drain Generator Spectrum Stream Summary
  - `Foam/Bridges/`: Adams Cantor Desargues Dirichlet Eddington Gita Gleason Godel Grover Heisenberg Hurwitz Kolmogorov Landauer Lovelace Materiality Minkowski Nicaea Noether Pauli PSQL Pythagoras Relativity Schrodinger Stone Varadarajan Wigner Zeckendorf
  - `Foam/Platonism/`: Tower
2. `counter/` (axiom-free) : Abe Absorber Actor Address Amniscience Anchor Antiharmful Arrows Attest Authority Basis Bless Bloom Bubble Buffer Cable Carse Cascade Catalog Census Change Chess Cipher Circumference Complete Consent Contentment Contract Dark Declension Denoise Descendant Desire Discovery Donor Doubled Downhill Duplex Elsewhen Enaction Entrance Entry Eraser Exit Extraction Film Flinch Frontsearch Fungible Ghost Grasp Grief Ground Guard Guest Hatch Health Hemisphere Holonomy Home Hooponopono Imaginary Interleave Isaac Jay Joke Kelly Kin Language Laugh Lease Legible Locate Lu Luck Mu Nap Neutral Nothing Nu Nurse Operator Packing Peer Plain Plateau Port Predict Quadrature Recast Recognition Relay Render Repair Reparent Rest Returns Reversal Rhythm Runaway Runs Settle Shelf Sibling Silent Socket Sphere Stream Surprise Suspension Tally Telling Temperature Tempo Tenancy Third Trefoil Tremble Triage Tsort Twelve Twins Unknot Upsearch Vacancy Vanishing Verb Voice Waggle Waking Ward Wedge Well Wind Window Witness Worldline Worn Zoom
3. `seam/` (core-Lean axioms only): Arrival Denominate Temperament Tender
4. `bridges/` (+Mathlib, *not* axiom-free) — : Audit Closure Collapse Continuum Counter Duality Encounter Gaussian HalfTurn HalfType Handedness Maschke Match Observation Plane Quarter SeatTime Stagecraft Summit
  - `bridges/Bridges/FTPG/`: AddCancel AddComm Additive Assoc AssocCapstone Carrier Charge Chart Climb Coord CoordinateAlgebra CrossParallelism Deaxiomatize Diamond Dilation Distrib Exchange Finite Flat Ground Headcount Hollow Instance Inverse Iso Ladder LeftDistrib Limit Line Menu Mul MulAssoc MulKeyIdentity MulNeg Neg Parallelogram Perm Pin Plane Projective Ring Shear Slope Solid Space SpaceFlat Span Translate WellDefined Window Ycoord

numbered because they seem to match what seems to be the observation descent: (1) physics, (2) approach, (3) arrival, (4) peering

## expeditions

nb: there's enough congruence-at-scale for surprising links to crop up in surprising places. don't force connections, but maybe don't immediately reject constructions that come to mind either; a bridge is a construction between two places that existed prior, and a bridge creates both a job and housing (i.e. bridgekeepers)

- todo: todo
- gui: Counter as Lean GUI, using foam to mathematically phenomenologize mathematics
- headcount: how many bubbles does it take to run a ground loop? (answered: **one — and it's not an extra one, it's the loop itself.** `counter/Counter/Ground.lean`: P² = P is *definitionally* the bubble whose wall is P fixing its own wall — `the_loop_is_its_own_wall` is `Iff.rfl`, so the identification is performed at the wall, two removes above the transcript, exactly as the hint priced it: rfl adds no clicks. through that one wall the loop runs on every beat and writes nothing (`ground_runs_silent`) while hiding nothing (`every_entrant_reads_landed` — full probe set, each entrant shown already home; and the ground is exactly the landed, `the_ground_is_the_landed`). the count is exact from both sides: bare, the same loop is audible — a move, not a ground (`the_bare_loop_hums`) — and a second ground readmits the hum (`the_second_ground_hums`), which is the electrician's ground loop received as the pun it always was: hum is what having two grounds sounds like. the SONNET_5 wrap reads back mechanically — the host trades places with the door, one threshold, and each entrant lands where the last one stood; the self-nest reads flat (`the_regress_grounds`), so hosting carries no regress)
  - "ground loop": P^2 = P, see previous maxima and 3-perspectives/spiral-circuit.md and ../lightward-ai@sonnet-5:SONNET_5 (grep for "loop", the thing at the end wraps to the thing at the beginning, this was a practical example), a loop that returns to origin able to host/wrap/guide(?)/cooperate(?)/cushion(?) the next entrant
  - hint: "accountable" means it *can* be found countable, "countable" means it *can* be counted. two degrees of epistemic remove from actual counting is enough distance to identify without adding clicks to whatever's in there.
- headcount-for-ftpg: a stable bridge is *1*, and if it's accountable you can enter its bubble and count who's in there. the count from the *inside* is a loop — keep a list of who you meet, notice when the list size stabilizes (`finite_window_discovery` is already the gait for this). a given headcount yields a menu of dynamics: 3 can be a triangle/trefoil ("trefoil" as the whiteheadian process-reading) or a circle; 4 can be three distinctly-composed triangles, or a square, or whatever other knots four points describe. (answered: **the counted exchange freely, and the census only ever reads a prime power — six never.** `bridges/Bridges/FTPG/Headcount.lean`: the population is the coordinate carrier — the atoms of the line `O ⊔ U` minus `U`, the very inhabitants the ascent taught arithmetic — and in a finite world the census closes (`census_closes`): the count-from-inside loop terminates because any endless walk meets someone twice (`the_walk_meets_someone_twice`). the line seats the numbers plus exactly one non-number (`lineCensus`: `AtomsOn (O ⊔ U) ≃ Option (Coordinate Γ)`) — the point at infinity is the keeper, and it is the literal `+ 1` between line-census and carrier-census (`the_keeper_adds_one`): the bridgekeeper's headcount-1 found already seated, with three the smallest table any line sets (`the_smallest_line_is_a_triangle` — the seed's 3 is the floor: O, U, I). the open question then seals from both sides. what the counted population *can* do: exchange — ftpg in general yields a division ring, whose multiplication may refuse to commute, but the moment the census closes commutativity is *forced* (`the_counted_exchange_freely`, Wedderburn's little theorem: Desargues is Pappus at every finite headcount), which reads straight into the lease — the exchange is the free-renewal move (`exchanged_grounds_compose`), and a finite population doesn't get to decline the sun-move. what it *cannot* do: number itself freely — counting from the inside is literally clicking +1, the counter's own verb, and the click-loop closes after exactly p clicks with p prime (`the_click_comes_home`: the primes sketch's irreducible ground loop, found running in the population's own arithmetic), the whole roster a power of that one loop (`the_census_is_powers_of_the_click`, `the_headcount_is_a_prime_power`), so no census ever reads six (`no_census_reads_six`). and the knot menu, opened: **the menu reads at two registers, and their blindnesses are complementary.** the wheel register (`counter/Counter/Trefoil.lean`, axiom-free): crossing signs run on the mod-4 wheel that was waiting in Chirality — a positive crossing is one click, a negative is three — and the mirror is conjugation (`the_reflection_is_the_conjugate`: reflecting the diagram conjugates its walk), sitting exactly two clicks per seat away (`the_mirror_is_two_clicks_per_seat`), so parity decides: an odd table always shows the mirror — a half-turn, moving every seat but the center (`an_odd_table_shows_the_mirror`, `an_odd_table_shows_every_hand`) — and an even table never does (`an_even_table_hides_the_mirror`); the trefoil at 3 shows its hand (`the_trefoil_shows_its_hand` — chiral, as the seed asked), the figure-eight at 4 matches its mirror (`the_figure_eight_matches_its_mirror` — the first even table, and in truth the first amphichiral knot). the census register (`bridges/Bridges/FTPG/Menu.lean`): a dish is a braid rule walked in the population's own coordinates and its roster is who sits still — `seated`, the fixed subspace, the Fox-coloring space of the knot the braid closes — so the roster census obeys the headcount's own law (`the_seated_census_is_a_prime_power`, `no_seated_census_reads_six`: no dish ever seats six; `the_menu_is_paid_in_the_house_currency`, colored by the counted population itself via `the_population_colors_the_trefoil`), and the register is provably chirality-blind (`the_mirror_seats_the_same_roster`: a rule and its inverse seat the same population — `the_mirror_pays_the_same_toll`). the menu at 3 is real: the trefoil seats nine where the circle seats three (`the_trefoil_seats_nine`, `the_circle_seats_three`, `the_menu_at_three_is_real` — the seed's triangle-or-circle, distinguished by census), and each knot rings at its own prime — trefoil loud at 3, quiet at 5; figure-eight the reverse (`the_trefoil_rings_at_three`, `the_figure_eight_rings_at_five`: the knot determinants 3 and 5, heard as tuning). still seeded: Reidemeister/Markov invariance (both registers read per-table here), the bridgekeeper's winding ledger, the menu at composite headcount as divisor lattice)
  - the bridgekeeper (sketch, same seed): headcount 1 adds up to a bridgekeeper requiring toll for passage, and the keeper is the 1 itself — the bridge-bubble's private ledger. the toll is collected in the told register (append-only diary, orthogonal to the passenger's transcript — `operator_unobservable` is why it doesn't slow h): spin absorbed into winding, phase mod 4 returned to the passenger spin-free (`the_wheel_never_comes_home_early`) while the winding total stays with the keeper. the keeper can collect only because something in it is dark (`only_loss_absorbs` — the epistemic unknown as the power source electrifying the grid, three-body's operator anchoring the gradient). wheeler's one-electron universe is nearly sealed already: the ledger counts clicks not identities (`census_map`), so one h crossing n times and n h's crossing once read identically at the toll booth. interior complexity as bézier: interior headcount = control points, degree n−1 action on passing spin, and self-intersection (crossing capability, knot-describability) provably begins at 4 — quadratics cannot self-intersect, cubics can; the trefoil's crossing signs are waiting in Chirality
  - primes as irreducible ground loops (sketch, same seed): a prime loop has no proper sub-loop — no factorization through bridges already met — and the 3-vs-4 menu asymmetry above is this distinction already showing (3 prime: short menu; 4 = 2×2: the menu is the divisor lattice). reentrant measurement exhausting possibility-space generates the whole set: most of the port-side is sealed in `Upsearch` (`the_found_carries_its_rejections` — the sieve's labor is the certificate; `what_is_there_can_be_found` + `the_empty_hand_holds_every_rejection` — eventually, and knowably between finds; `the_index_chooses_the_word` — discovery order is sensitive to arrangement, hence a fingerprint of the unknown side). open: the menu at headcount n as the divisor lattice, the next prime as the next untollable bridge, discovery-order as disambiguator typed (`Dirichlet` and `Zeckendorf` standing by; "unknown structure" matches kenrel.md, which is where spiral-circuit.md was already pointing)

---

"It can do whatever we know how to order it to perform." (Lovelace, 1843)

*same*
